# AI 3D Home — 开发日志

> 目标：做一个自己的加载项「AI 3D Home」，功能和 UI 布局与 JMGLink 3D Home 一致，先参照复刻、后期再优化。
> 原则：一步一步来、做好记录、每一步稳、不确定就停下来沟通。

## 2026-08-12 18:10 · 原版 UI 研究（截图 + DOM 分析，参照已存 /tmp/ref_*.png）

### 一、主界面（查看器）
- 全屏 3D 画布（无侧栏）
- 左上角：连接状态「Home Assistant 已连接」
- 底部工具栏（从左到右）：
  - 控制组：旋转 / 高清(画质) / 玻璃 / 设备N(设备计数) / 绑定 / 编辑 / 设置
  - 模式组：全屋 / 照明 / 遮阳 / 环境 / 安防
- 「绑定」→ 右侧实体选择抽屉（按分类：全部/灯光/开关/窗帘/空调/传感器/感应器/摄像机/风扇/安防，带数量）

### 二、编辑器界面（点「编辑」进入）
- 顶部工具栏：
  - 工具组：全屋(模式) / 参考底图 / 导入JSON / 导出JSON / 最近备份 / 重置当前户型 / Area绑定建议
  - 家具库：沙发2.2×0.9 / 床1.8×2 / 餐桌1.4×0.82 / 书桌1.2×0.6 / 衣柜1.4×0.6 / 橱柜1.6×0.6 / 岛台1.8×0.9 / 茶几1×0.55 / 书架1.1×0.32
  - 设备分类：全部506/灯光22/开关339/窗帘14/空调5/传感器94/感应器24/摄像机2/风扇4/安防2
  - 视图：吸附 / 尺寸 / 标签 / 2D / 3D / HA已连接 / 已保存
- 左侧竖排工具：选择V / 平移H / 墙体W / 门D / 窗N / 家具F / 设备E / 贴图B / 删除Del
- 底部：缩放 -/+ / 居中 / 复制当前层 / 删除楼层

### 三、技术栈（从 bundle 逆向）
- 前端：React + React Three Fiber（程序化建模，非 glTF）
- 后端：Node + Fastify，WebSocket gateway，SUPERVISOR_TOKEN 鉴权，数据存 /data
- 数据模型：floors[] → rooms[](多边形)/furniture[]/devices[]/openings[]
- 质量预设：eco/smooth/balanced/high（dpr 与阴影分辨率分级）
- 渲染模式：全屋/照明/遮阳/环境/安防 + 日/夜 + 自动旋转

## 2026-08-12 19:40 · 阶段0-3 完成（本地 playwright 12 项测试全过）

- **技术栈**：React 18 + React Three Fiber + three r170，Vite 构建（base:'./' 适配 ingress），后端 Python 标准库（自包含构建，HA 不需要 npm）
- **已完成**：
  - 阶段0：加载项骨架（config/build/Dockerfile/run.sh）+ 后端 server.py（静态服务+HA 代理+项目存取+状态缓存）
  - 阶段1：3D 渲染核心（房间地板/墙体/家具/设备标记，OrbitControls，画质预设 eco~high，自动旋转，日/夜）
  - 阶段2：编辑器（顶部工具栏/左侧竖排工具/底部楼层管理，画墙生成房间，导入导出 JSON，吸附/标签/2D-3D 切换）
  - 阶段3：设备绑定抽屉（分类+搜索）、实时状态（灯变暖黄/传感器数值）、点击控制弹窗
  - 阶段4部分：渲染模式（全屋/照明/遮阳/环境/安防）光照差异
- **12 项 playwright 测试**：底部栏/连接状态/画布/编辑器/画墙/绑定/放设备/模式切换，全部通过，无控制台错误
- **踩坑记录**：
  1. R3F hooks（useThree/useFrame）必须在 Canvas 组件内——EditorController 曾在外面导致报错
  2. `useThree().controls` 是 null（手动建 OrbitControls 不挂 R3F 状态）→ 相机目标改用 store 传递
  3. 放置工具（墙/家具/设备）时房间/家具/设备必须不拦截点击（interactive=false），否则点房间里放不下设备
  4. Vite `base:'./'` 让产物用相对路径，HA ingress 下才能加载

## 2026-08-12 21:40 · 阶段5 部署完成（AI 3D Home 已在 HA 运行）

- **部署**：仓库 `github.com/AIone-1/hassio-addon-ai-3d-home`（私有），HA 上 slug=`981c17cd_ai_3d_home`，侧边栏「AI 3D Home」已开
- **⚠️ 关键发现：这台 HA 不注入 SUPERVISOR_TOKEN**（容器 env 只有 PATH/PWD/TZ 等 5 个，无 SUPERVISOR_TOKEN/HASSIO_TOKEN）。小爱播报也是靠**手动配置 ha_host + ha_token** 直连 HA 绕过的。**AI 3D Home 同样需要手动在加载项 options 里填 ha_host/ha_token**，否则连不上 HA（401）
- **踩坑补充**：
  1. `useSyncExternalStore` 的 selector 不能返回新对象（`|| []` 每次新建 → React error #185 无限循环）→ 用模块级常量 `NO_DEVICES`
  2. HA ingress 下前端 API 必须用相对路径（`location.pathname` 拼前缀），绝对路径 `/api/...` 打到 HA 根 → 404。Vite `base:'./'` 只管静态资源
  3. supervisor 的 `POST /addons/{slug}/options`：加载项自身 options 要嵌套在 `options` 键下（顶层只认 boot/ingress_panel 等通用字段）
  4. 重装（uninstall）会清掉 options 和面板设置，重装后要重新配 token + 开面板

## 2026-08-12 22:15 · 用户反馈"画不了墙"，修复两个致命 bug（v0.1.0-p8）

- **bug1（画不了）**：HA 上项目是空的（`floors:[]`），`currentFloor()` 返回 null → EditorController 直接 return null → **交互平面不渲染 → 点画布完全没反应**。修复：启动时无楼层自动创建默认楼层
- **bug2（React #310）**：EditorController 在 `if(!floor) return null` 之后还有 `useMemo` —— floor 从空变有时 hook 数量变化崩溃。修复：删掉多余的 useMemo
- **✅ 部署版实测**：编辑 → 墙体 → 点 4 点 → Enter → 成功生成房间（房间1）。HA 连接正常
- **重要**：部署脚本必须**先转公开再重装**（仓库私有 supervisor 拉不到），重装后要重新配 token + 开面板
- **诚实说明**：当前 UI 是功能骨架，与原版 JMGLink 的精致度还有差距（图标/间距/视觉细节），需后续迭代打磨

## 2026-08-12 23:00 · 用户反馈多项问题，修一批 UX bug（v0.1.0-p11）

- **用户反馈**：画不了墙、很多按钮点了没反应、家具显示看不清
- **关键诊断**：场景确实在渲染（329帧/830三角形），截图黑是 headless 合成问题；问题在交互反馈和 UX
- **修复**：
  1. **画墙立即生成可见墙段**（每点一下出一段黄色墙，对齐原版行为；点回起点或 Enter 闭合自动生成房间）
  2. **死按钮灰掉**（设置/璃玻璃 disabled，不再点了没反应）
  3. **提亮默认灯光**（看清家具）
  4. **墙体工具加 toast 使用提示**（"点画布放墙点，点回起点或按 Enter 闭合"）
  5. **修 toast 命名冲突**：App.jsx 里 `const toast = useStore(s=>s.toast)` 遮住了导入的 `toast()` 函数 → 调用报 "o is not a function"。改成 `toastMsg`
- **✅ 部署版实测**：点4点 → 墙段数=3（立即可见）→ Enter → 房间数=1，无控制台错误
- **⚠️ 部署脚本必须**：先转公开 → 重装 → 配 token → 开面板 → 转私有（一条龙 `/tmp/ha_deploy_ai3d.py`）

## 2026-08-13 00:10 · 用户严肃批评：不要瞎改，要照原版做（v0.1.0-p14）

- **用户批评要点**：①我自作主张改（黄色墙≠原版毛玻璃）；②2D/3D 切换是真功能我没做；③删除要能做；④很多按钮不能用
- **对照原版代码确认**：原版墙体用 MeshPhysicalMaterial + clearcoat（毛玻璃、透明）；2D/3D 是真视图切换（`te==="2d"`，俯视画图再切 3D）
- **修复（p14 实测通过）**：
  1. 墙体改毛玻璃：`meshPhysicalMaterial transparent opacity .35-.45 clearcoat 1`（对齐原版）
  2. 2D 模式做真：俯视相机（y=28 往下看）+ 2D 禁旋转只平移缩放；切 3D 恢复透视。空项目也能切
  3. 删除工具做真：删除模式点墙段/房间/家具/设备即删（墙段 3→2 实测）
  4. 修 MOUSE 常量（OrbitControls.MOUSE 不存在，从 three 导入）
- **⚠️ 教训（写死）**：**做这个加载项必须照原版 bundle 抄，不许自己发明**。每个功能先看原版怎么写（/tmp/3dhome_assets/index_beautified.js），再对齐实现

## 2026-08-13 01:00 · 用户教精髓，对齐原版 2D（v0.1.0-p17）

- **用户讲的精髓（照做）**：①2D 是真正的正交平面（不是 3D 俯视）；②画墙是"点一下→拖出虚线预览→再点生成"；③编辑界面带网格；④编辑完回到正常地面
- **实现（p17 实测通过）**：
  1. **真 2D 正交相机**：R3F `set({camera})` 换 THREE.OrthographicCamera（俯视平面），3D 换回透视。验证 activeCam: ortho/persp ✓
  2. **画墙虚线预览**：onPointerMove 更新 Line 的虚线（dashSize 0.3），computeLineDistances 要调在 line 对象上（不是 geometry）——踩坑
  3. **编辑网格更明显**：编辑时网格 40×40、颜色更浅；2D 时铺浅色地面平面
  4. 毛玻璃墙、删除工具继续生效
- **⚠️ 部署教训**：仓库私有导致 supervisor 拉取失败多次。**固定用 `/tmp/ha_deploy_all.sh`（转公开→重装→配token→开面板→转私有）一条龙**，别再手动三步
- **R3F 相机坑**：onCreated 闭包里的 camera 是初始透视相机，换相机后要用 `window.__dbg3d.activeCam` 探针（由 CameraSwitcher 设置），不能读闭包 camera

## 2026-08-13 10:55 · 移动工具独立 + 共享墙去重

- **移动工具**：家具拖动从「选择」工具里拆出来，独立成「移动」工具（左侧 M 键 / 快捷键 M）
  - `canDrag = tool === 'move' && editing`（必须带 editing 守卫，否则退出编辑后查看模式还能误拖家具）
  - **退出编辑时重置 `tool:'select'`**（Escape 和底部「退出编辑」两处都要）——否则 tool 残留成 wall/move 时，查看模式下 `interactive=false` 导致设备点不动、家具还能拖
- **共享墙去重**：相邻两个房间共用的那堵墙，原来每个房间各画一遍 → 两遍重叠（透明玻璃墙变厚变暗）
  - 新增 `wallKey(seg)`（忽略方向 + 四舍五入 2 位），把墙渲染从 `Room` 组件上移到 `Scene` 层，用 Map 按 key 去重，只画一次
  - 去重时记录 `roomIds`（一堵墙可能归属多个房间），选中任一房间该墙都高亮，行为与原版一致
  - 墙高 h 取首个归属房间的高度（当前所有房间统一 2.8m，无差异）
- **测试**：`wallKey` 方向无关 + 同向/反向共享边去重，node 脚本验证通过（4+4 房→7 墙段）；build 通过

## 2026-08-13 11:14 · 修 2D 编辑"不规整"（受 3D 旋转/移动影响）

- **用户反馈**：编辑界面要一直是规整的，不要受移动/旋转位置影响；让我学别人怎么画 2D 转 3D
- **研究原版**：JMGLink 的 2D 编辑是**独立 SVG 画布**（`<svg className="floorplan-canvas">`），跟 3D WebGL 完全隔离，天然规整；切 2D 时 `Ts()` 重置 zoom=1 + pan=[0,0]。我的实现用 3D 正交相机冒充 2D，所以会被 3D 相机影响
- **找到两个真 bug（当前正交相机方案）**：
  1. **OrbitControls 重建丢状态**：切 2D/3D 相机时 `set({camera})` 会让 Controls 的 `[camera,gl]` effect 重建 OrbitControls，默认 `target=(0,0,0)`、`enableRotate=true` —— 没继承当前状态 → 2D 视图偏到原点 + **还能被旋转**（不规整的根因）
  2. **正交相机 zoom 没重置**：2D 里缩放过、切 3D 再切回来，`camera.zoom` 残留 → 缩放没回位
- **修复**：①Controls 重建时立刻用 `getState()` 读当前 camTarget/enableRotate/mouseButtons 重置；②CameraSwitcher 进入 2D 时 `ortho.zoom = 1`
- **⚠️ 未来方向（记下）**：若要做"完全规整的 2D 编辑"，正确做法是像原版那样**独立 SVG 画布**，而不是 3D 正交相机。当前先用手头正交方案 + 相机重置顶着

## 2026-08-13 12:00 · 2D 编辑器重写为独立 SVG 画布（对齐 JMGLink 原版，用户选 B 方案）

- **用户拍板**："要做就做最好的，不能糊弄；动手前把资料吃透"。2D 编辑彻底重写为**独立 SVG 画布**，与 3D WebGL 完全隔离，永远规整、不受 3D 旋转/移动影响
- **吃透原版后的关键机制**（都对齐了）：
  1. 2D 是独立 `<svg>`，`viewBox` 控制缩放平移（`viewBox=[中心+pan, 尺寸/zoom]`，`preserveAspectRatio` 保持不拉伸）
  2. 坐标转换用 `createSVGPoint()` + `matrixTransform(getScreenCTM().inverse())`——不用手动算缩放
  3. 缩放到光标（原版 `pi`）+ 居中重置（原版 `Ts`，zoom=1/pan=[0,0]）
  4. 网格用 SVG `<pattern>`（小格 0.5m / 大格 1m，`userSpaceOnUse` 对齐世界坐标）
  5. 房间 polygon+名字+面积（`m²`）、墙 line、家具 rect+标签、设备圆点+标签
  6. 画墙**轴向吸附**（水平/垂直，让房间规整）+ 网格吸附 0.5m + 点回起点/Enter 闭合
- **新增**：`PlanEditor.jsx`（约 400 行）。App 按 `view2d` 渲染 SVG 或 WebGL；`view2d=true` → PlanEditor，`false` → Viewer
- **移除**：正交相机 `CameraSwitcher`、3D 交互 `EditorController`（2D 全走 SVG，EditorController 成死代码暂留）
- **视觉校准对齐原版**：room-name 字号 0.24 + 白描边(paint-order)、room-area 0.17、家具浅底+彩色描边、家具标签 0.16
- **⚠️ 踩坑（测试发现）**：`api.js` 里 `BASE`（尾斜杠）+ path（前斜杠）拼成 `//api/project`，浏览器当成**协议相对 URL**（host=api）→ 根路径本地测试时接口全挂。HA ingress 非根路径下不暴露。修复：`req` 里 `(BASE+path).replace(/\/{2,}/g,'/')`
- **⚠️ 交互细节**：元素 `onClick` 只在 select/delete 工具下拦截，画墙/放家具/放设备时让点击穿透到背景（否则点已有房间会被误选中）
- **✅ 本地 playwright 冒烟测试**：初始 3D canvas=1 → 进编辑 SVG=1、房间/墙/家具/网格渲染正确 → 画墙成房（房间数+1）→ 滚轮缩放 viewBox 变化 → 切 3D 回 canvas，全程无页面错误

## 2026-08-13 13:30 · 墙改线段主数据 + 房间自动检测（共用墙/相交封闭）+ 中心点 + 移动户型

- **用户反馈三点**：①原版两个房间共用一堵墙也能封闭，我这个两条线相交形成不了封闭；②2D 加中心点（否则 3D 旋转偏移很大）；③加「移动户型」拖整个户型
- **核心机制（吃透原版 $l/_l 多边形化）**：墙是线段（`floor.walls` 持久化主数据），房间是**从墙段的封闭环自动检测**出来的（`detectRooms`：墙在交点处分割 → 建平面图 → 面遍历(右手规则)找封闭环 → 每个有符号面积>0.25 的环就是一个房间）。**这就是"共用墙/相交也封闭"**——任何闭环自动成房间，两个房间共用一堵墙就是两个闭环
- **数据模型反转**：原来房间存多边形、墙派生；现在墙是主数据、房间 `recomputeRooms` 自动检测（按质心重叠匹配旧房间保留 id/名字/颜色）
- **实现**：`detectRooms`/`recomputeRooms`/`roomsToWalls`/`centroid`/`pointInPolygon` 加到 geometry.js；画墙每加一段→自动重算房间；加载时迁移旧数据（房间→墙）+ 一致化（墙→房间）
- **⚠️ 踩坑**：面遍历要用**有符号面积**过滤（`>0.25`），不能用绝对值——绝对值会把外环/反向重复面也算进去（矩形会测出 2 个房间）
- **中心点**：2D 画布原点画十字标记（`.plan-origin`），原点 = 3D 旋转中心
- **移动户型**：新工具 `movePlan`（G 键），拖整个户型（房间 points/墙 start/end/家具/设备一起平移）
- **修旋转偏移**：自动旋转从「手动转 rootRef（绕原点）」改成 OrbitControls `autoRotate`（绕 target=户型中心）
- **✅ 测试**：node 测 detectRooms（矩形1/共用墙2/中间墙切2/L形1/平行线0）；playwright 测共用墙渲染（房间2墙7）、移动户型、切3D 自动旋转无错；部署版 4 房间旧数据迁移成功

## 2026-08-13 14:10 · 对齐原版默认视角 + 毛玻璃墙 + 地板颜色

- **用户反馈**：①首页默认视图角度"偏上"；②毛玻璃和原版不一样；③地板颜色不一样。强调"自己做不好就学别人的，一定要吃透它的代码"
- **吃透原版拿到精确值（glass 风格 day 主题）**：
  1. **相机**：`lg` 组件 iso 视角——`camera = 户型中心 + (0.86b, 0.86b, 1.04b)`，`b = distance×0.88`（桌面），`fov=36`；`distance = max(9.5, 宽×1.42, 高×1.7, 层高×1.3)`（`ig` 函数）。目标 Y=0.55。我原来用 `size×1.6+12` 高俯视（~63°），改成这个后 ~29° 斜对角
  2. **墙（毛玻璃）**：`meshPhysicalMaterial` roughness 0.5 / metalness 0 / clearcoat 0.16 / clearcoatRoughness 0.72 / transparent / opacity=wallOpacity×0.82=0.197 / depthWrite=false，色 `#d5e0f1`（day）/ `#d0dcee`（night）
  3. **地板**：`meshPhysicalMaterial` roughness 0.46 / clearcoat 0.2 / clearcoatRoughness 0.82 / metalness 0.02，冷色调色板 `["#7789ad","#8294b7","#8a9bbd","#7285aa","#7e91b5"]` 每房间按序号取色（`recomputeRooms`）
- **⚠️ 关键**：原版主题分 style（glass/cartoon/cream）+ theme（day/night），`Ua()` 决定。默认 `visualStyle="glass"` + `theme="day"` → `_0` 主题（冷色蓝灰）。毛玻璃=半透明(0.2)+低 clearcoat(0.16)，不是高透明高 clearcoat
- **✅ 测试**：相机位置 [9.2,7.7,10.2]（公式验证），房间渲染无错误，已部署

## 2026-08-13 14:30 · 对齐原版背景色 + 限制旋转不翻底面

- **用户反馈**：主页背景色也要参照；3D 户型图不能转到"底面向上"
- **背景/雾色（原版 glass 风格 day，五模式）**：
  - 全屋 background `#5278ae` / fog `#6d8ebe`
  - 照明 `#5879ad`/`#6685b4`；遮阳 `#080c22`/`#0c122b`；环境 `#4f74aa`/`#6889ba`；安防 `#070a1d`/`#0b0f27`
  - 之前我用的 `#e9eef2` 浅灰是错的——原版是蓝色（`Ua("day","overview","glass")` → `_0` 主题 background `#5278ae`）
- **⚠️ 验证手段**：用 PIL 采样参考截图四角/垂直方向，确认原版是蓝色渐变（顶 #395380 → 底 #58709c），不是浅灰
- **旋转约束**：OrbitControls 加 `minPolarAngle=0.05` / `maxPolarAngle=π/2-0.05`，相机不能翻到地面以下（否则户型底面朝上、穿模）

## 2026-08-13 14:03 · 渐变背景 + 地板同高 + 旋转顺逆时针 + 门窗放置

- **用户反馈**：①背景是渐变不是纯色；②地板高低不一样；③旋转要顺/逆时针；④门窗按钮不好用；⑤2D 画图房间面积不显示（刷新才显示）
- **背景渐变**：PIL 采样参考图（三张一致）拿到天空垂直渐变 顶 `#253962` → 底 `#46618d`，用 CanvasTexture 实现，替换原纯色
- **地板同高**：原 `floorY = level + roomIdx*0.05`（按房间序号错落 5cm）是旧房间模型防 z-fighting 的遗留。新 detectRooms 模型下房间不重叠，改成统一 `level + 0.025`（对齐原版）
- **旋转方向**：store 加 `rotateDir`，OrbitControls `autoRotateSpeed = 1.2 * rotateDir`；旋转按钮三态循环（停止→顺时针→逆时针）
- **门窗放置**：`pointToSeg`（点到线段投影）找最近墙，点墙放门(0.9m)/窗(1.2m)，存 `floor.openings`（新模型 wallId+offset），SVG+3D 渲染，删除模式可删；3D Opening 组件从旧 roomId/wallIndex 改成 wallId
- **面积不显示**：本地复现时面积正常显示（"6.0 m²"），疑似对比度低（灰色 #9299a0 在浅色地板看不清）——加深房间名/面积文字色到 #4a5560 + 微调字号；并修 bounds useMemo 依赖（改用 JSON.stringify，原地 push 墙时不重算导致视口不重拟合）
- **⚠️ 教训**：SVG 文字 `paint-order:stroke` + 白描边在浅色地板上需要更深的 fill 才可读

## 2026-08-13 15:10 · 修共用墙画不了第二个房间 + 门窗渲染对齐原版

- **用户反馈**：单独圈一个房间能立即显示，但用公共墙画第二个房间时不显示、刷新才显示；门窗实际效果要参照样例不要自己编
- **⚠️ 根因（自己埋的坑）**：之前"防御性修复"把正在画的墙也纳入 bounds 取景 + JSON.stringify 依赖，导致**画完第一个房间后视口立刻缩到房间边缘，相邻房间的墙点跑到屏幕外点不到**——第二个房间根本没画进去
- **修复**：bounds 只按**已完成房间**（+家具/设备）取景，四周留 **5m 边距**，画图过程视口稳定；撤回 JSON.stringify 依赖（房间由 recomputeRooms 返回新数组，引用变化自然触发重算）
- **门窗渲染对齐原版**（照抄 CSS+几何）：
  - 墙洞 `.opening-track`：白粗线 0.25 盖住墙
  - 门线 `.opening-line.door` #f8f4ee / 窗线 `.window` #d8edf3 宽 0.16
  - 门扇 `.door-leaf`（线）+ 开启弧 `.door-swing`（弧 path，半径=门宽）
- **✅ 测试**：共用墙画两房 → 2D 立即渲染 2 房间+2 面积；门/窗渲染门扇/门弧/窗线齐全

## 2026-08-13 20:43 · FPS 计数器改直接写 DOM + 补记性能优化主线

### 本次改动（接上个会话爆掉前留下的半成品）
- **现场**：上个会话把 FPS/卡顿计数器从 `useState`（每秒 setState → 每秒重渲染制造垃圾）改成 `useRef` 直接写 DOM，改到一半——删了 useState、加了 useRef、useEffect 写 DOM 逻辑都写好，**但 JSX 还残留 `{fps}`/`{maxStutter}` 引用**（这俩 state 已删）→ 一跑就 ReferenceError 崩溃
- **补完**：JSX 合并成一个 `<span ref={fpsRef}/>`，useEffect 每秒直接写「N FPS · 卡顿 Xms」+ 按帧率变色（≥50 绿 / ≥30 黄 / 其余红）
- **验证**：`npm run build` 通过；grep 产物确认旧 `maxStutter`/「最大卡顿」独立逻辑清干净、新「FPS · 卡顿 Xms」+动态色已进产物
- **部署**：commit `7bbc51e` → push → `python3 /tmp/ha_deploy_ai3d.py` 重装（v0.1.0-p40，健康 True）

### 补记：15:10 之后到现在的性能优化主线（之前没写日志，这次靠 git log 才拼出进度）
- 15:45 净化器改方形米家款 + 马桶重做
- 16:18-16:55 支持加载网上 GLB 3D 家具模型 + 批量集成 123 个 + 缩略图选择器 + 修中文文件名 404 / 缩略图问号
- 17:36-18:30 家具库分类折叠+尺寸选择、主界面转速调节、**转动卡顿排查**（HA 状态轮询改轻量指纹对比、自动旋转时暂停轮询、转速帧率无关）
- 18:57-20:22 新增 96 个家具模型、FPS 计数器诊断、画质/性能优化（DPR 降档、关抗锯齿、DeviceLabels GC 优化）

### 教训
- 上下文爆掉后，进度只能靠 `git log` 恢复，开发日志没跟上 → **每做完一个明确小功能就写日志 + commit，别攒到上下文爆了才补**

## 2026-08-13 20:51 · 卡顿真相（远程访问软件）+ 画质拉满

- **卡顿真相（用户实测，重要）**：之前几小时排查的「转动卡顿」，根因**不是浏览器 / 不是 HA / 不是配置太高**，是用户**远程访问软件本身卡**（远程桌面编码/传输卡）。之前为「卡顿」做的那批降配全是误伤
- **画质拉满**（用户明确「极高、最高、直接拉满」）：
  - 默认画质 `balanced` → `high`（store.js + App.jsx 加载默认，两处）
  - `high` 档 dprMax `2.0` → `3.0`（覆盖手机最高 DPR，等于不限制）
  - 抗锯齿 aa=true（本来 high 就开）
  - shadow map `1024` → `2048`（Scene.jsx 写死值；QUALITY.shadow 字段是死字段一直没接线，这次直接把写死值对齐 high 档）
- **部署**：commit `c129849` → push → 重装 v0.1.0-p40，健康 True
- **教训**：排查「卡顿/性能」先怀疑**链路最薄弱环节（远程访问/网络）**，别急着降画质——降配只会让真凶躲起来，还白损画质

## 2026-08-13 21:15 · 家具旋转 + 挂墙/挂顶（对齐原版 placement 机制）

- **需求**：编辑器里能①调整家具方向（旋转）②挂墙③挂顶。用户定：旋转=按钮+数字都要、挂墙挂顶=地面/墙面/屋顶三选一、家具=补新类型+现有也能调
- **吃透原版拿到机制**：
  1. 原版物品库 `io` 每个物品有 `placement`(floor/wall/ceiling) + `defaultHeight`(离地高度，开关 0.92/插座 0.28/窗帘 1.12/壁挂空调 1.32)
  2. 家具 `rotation` 是弧度，属性面板「旋转」数字输入框(°，step 5)，`round(rot*180/PI)` 显示
  3. 渲染 `uh(position, placement, ...)` 按 placement 算 3D 高度；2D 渲染 `rotate(rot*180/PI)`
  4. 原版家具全是地面；挂墙/挂顶是「设备」(light/switch/cover) 的 placement
- **实现**：
  1. **数据**：`FURNITURE_LIB` 每项加 `placement`；新增挂墙(壁灯/挂画)+挂顶(吊灯/吸顶灯/筒灯)共 5 个，家具库 17 个
  2. **3D 模型**：`FurnitureModel` 加 5 个 case——吊灯=吊线+朝下锥形灯罩、吸顶灯/筒灯=贴顶扁圆盘、壁灯=灯座+暖光球、挂画=薄板+画心
  3. **2D 编辑器**：放置按 placement 设初始高度(floor 0 / wall 1.2 / ceiling 层高)；选中家具弹出**属性面板**(右上角)——旋转(⟲⟳ 按钮 + 数字°)、放置面(地面/墙面/屋顶)、高度(挂墙挂顶时)、删除；挂墙挂顶家具 2D 虚线边框标记
  4. 现有家具 placement 默认 floor，也能通过面板切 wall/ceiling(旧数据兼容：无 placement 字段按 floor)
- **验证**：node 自检 17 个家具全有 placement+颜色 ✓；build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：原版 `placement`/`defaultHeight` 是「设备」的挂墙挂顶机制，家具本无此概念——用户口语「家具」涵盖挂墙挂顶物品，实现时统一给 furniture 加 placement 字段即可，不必拆家具/设备两套

## 2026-08-13 21:30 · 门窗增强：门 4 类型 + 5 色 + 内开外开 + 翻转；窗 4 类型

- **需求**：门加类型/门框/颜色/内开外开；窗加类型（落地窗等）。用户定：门=平开门+双开门+推拉门+门框、颜色=木/白/灰/黑/蓝 5 色、窗=普通+落地+推拉+飘窗
- **吃透原版拿到的字段**：`swing`(inward/outward 内开外开)、`hinge`(start/end 铰链左/右)、`windowStyle`(standard/floor_to_ceiling)、`bottom`(窗台高)。原版门=门扇/门洞+内开外开+翻转；窗=普通/落地。门类型/颜色/门框是原版没有的，自己设计
- **实现**：
  1. `geometry.js` 加 `DOOR_COLORS`(5 色) + `DOOR_STYLES`(swing/double/slide/frame) + `WINDOW_STYLES`(standard/floor_to_ceiling/slide/bay)
  2. `Scene.jsx` Opening 拆成 `Door3D` + `Window3D`：门=平开(单扇绕铰链微开)/双开(两扇对称)/推拉(两滑板)/门框(只框无扇)，门扇用 `op.color`，`swing`×`hinge` 决定微开方向；窗=普通(玻璃+四边框)/落地(到底)/推拉(两扇玻璃错位)/飘窗(前玻璃+左右斜玻璃+窗台板)
  3. `PlanEditor.jsx` 门窗 2D 按类型区分（双开两弧、推拉两线、门框无扇、门扇用门色）+ 选中门窗弹属性面板（门：类型/颜色/内开外开/翻转门扇；窗：类型，落地窗联动 bottom=0/height=层高）
  4. 放置门窗设默认字段（门 swing/木色/inward/start/2.1；窗 standard/0.9/0.9）
- **验证**：node 自检 5 色 + 4 门型 + 4 窗型 ✓；build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：门「开向」= `swing`(内/外) × `hinge`(铰链左/右) 两个正交字段，3D 里门扇微开方向 = leafDir(铰链决定门扇延伸向) × swingDir 的正负；窗「落地 vs 普通」切换要联动 `bottom`/`height`（落地 bottom=0、height=层高），别只改 windowStyle

## 2026-08-13 21:44 · 全屏按钮 + 纯户型图模式 + 吸附精度 + 保存加载防御修复

- **主界面全屏按钮**：BottomBar 控制组加「⛶ 全屏」，`requestFullscreen`/`exitFullscreen`（toggleFullscreen）
- **纯户型图模式**：MODES 加「纯户型」，只展示墙体/房间/门窗结构，隐藏家具+设备+设备标签（Scene.jsx 家具/设备 `mode !== '纯户型'` 守卫 + Viewer.jsx DeviceLabels 隐藏）
  - **旋转语义（用户两次澄清才懂）**：纯户型图模式**旋不旋转取决于进入前的状态**——切过去不主动改 autoRotate，之前转就继续转、之前停就停。不是强制停止
- **保存 bug 修复（防御）**：排查发现数据其实在 `/share/ai_3d_home/project.json`（15KB，房间 points 完整），但加载时 `recomputeRooms` 会无条件用墙重算覆盖保存的房间，一旦 detectRooms 返回空就把房间丢了。修复：重算返回空且原本有房间时，保留原房间
  - **排查路径**：SSH 到 HA（aione/111111）`cat /share/ai_3d_home/project.json` 看真实数据，确认数据在、定位到是加载覆盖问题，不是保存丢数据
- **吸附精度可调**：store 加 `snapStep`(默认 0.5)，编辑器顶部加「0.5m」循环按钮（0.1/0.25/0.5/1 四档），PlanEditor 吸附用 `snapStep` 替代硬编码 `SNAP=0.5`
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①排查「保存后显示不出来」先确认数据到底在不在（SSH 看文件），区分「没保存」和「加载时被覆盖」；②`recomputeRooms` 这种「重算覆盖」逻辑要加防御——重算失败就保留原值，别让检测函数的边界情况吞掉用户数据

## 2026-08-13 21:52 · 家具属性增强：名字/大小/颜色/复制 + 显示名字开关

- **需求**：选中家具弹窗加大小修改、自定义名字（不填用默认）、页面加显示名字开关；用户让我主动补功能
- **家具数据新增字段**：`name`(自定义名)、`color`(自定义色)、`width/depth/height`(自定义尺寸)。渲染 `name || type`、`color || 主题色`、`width || lib.w×scale`
- **3D 渲染**：`FurnitureModel` 加 `color/w/d/h` 参数，`M = color || FURNITURE_MAIN`，`D/A = mixColor(color, 黑/白)` 自动派生明暗——所有 case 不改就生效；`Furniture` 组件算 `w = item.width ?? lib.w×scale` 传给模型 + 选中框尺寸用 `h`
- **2D 渲染**：label 用 `name || type`，描边色 `color || FURNITURE_COLORS[type]`，尺寸 `width ?? lib.w×scale`
- **属性面板新增**：名字(文本框，placeholder=类型名)、大小(宽/深/高 三个数字框)、颜色(默认+8 色 swatch)、复制(⧉，JSON 深拷贝 + 偏移 0.3 复制一份)
- **显示名字开关**：store 加 `showFurnitureLabels`(默认 true)，编辑器顶部「名字」按钮，2D 家具标签按开关显示
- **主动补的功能**：家具颜色自定义(像门一样，3D 也变色)、家具复制(高频实用)
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：家具颜色要让 3D 也变，最省事是让主色可配 + 明暗色用 mixColor 派生（一个 color 参数，全部 case 生效），不用逐 case 改材质

## 2026-08-13 22:04 · 修 2D/3D 尺寸不一致 + 家具百分比等比缩放 + 纯净沉浸模式

- **修 2D/3D 家具占地不一致（真 bug）**：3D 里 `Furniture` 组件算 `w = lib.w × scale[0]` 传给模型，但外层 `<group scale={scale}>` 又缩放一次 → **scale 被乘了两次**；2D 只乘一次。修复：统一公式 `实际尺寸 = (width ?? lib.w) × scale`——3D 模型用基础尺寸（不乘 scale，组 scale 负责），2D rect 乘 scale
- **百分比等比缩放**：属性面板加「缩放 %」输入，`setFurnitureScale` 改 `scale=[s,s,s]`（等比），2D/3D 同步缩放。宽/深/高仍是非等比精确自定义（乘在 scale 上）
- **纯净沉浸模式**：store 加 `immersive`；BottomBar 加「👁 沉浸」按钮进入；App.jsx 里 `immersive` 时隐藏 status-tl、BottomBar 等所有外围 UI（只留 3D 场景+场景内标签），`.app` 上 `onDoubleClick` 双击退出
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①家具缩放用了 `scale` 字段，尺寸计算和渲染要「各管一段」——要么模型用最终尺寸、组不 scale，要么模型用基础尺寸、组 scale，**不能两处都乘**（这次就是两处乘导致 2D/3D 不一致）；②push 时 HTTPS 大包推送在网络抖动下常报 `SSL_ERROR_SYSCALL`/`send-pack disconnect`，curl 能通但 git 不通，**重试循环 3 次基本能推上去**

## 2026-08-13 22:18 · 交互改进：左键选择 / 双击拖动 / 右键取消 + 门窗沿墙拖动

- **双击拖动**：家具/设备/门窗加 `onDoubleClick` → `doubleClickDrag`（选中 + 切 move 工具），后续按住即可拖动，不用再切 M 工具
- **门窗沿墙拖动**：加 `openingDragRef`，move 工具下按住门窗 → `handleMove` 里 `pointToSeg` 找最近墙更新 `wallId`+`offset`，门窗沿墙滑动
- **右键取消**：右键 = 取消画墙草稿 + 取消删除/画墙工具（切回 select）+ 取消选中
- **床 2D/3D 尺寸**：读代码确认 2D/3D 公式已统一（`(width??lib)×scale`），床主 box 占地 1.8×2.0 与 2D 一致——用户看到的「床 2D 比 3D 大」是上一轮 scale 双重缩放修复前的旧版本，强刷即可
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-13 23:06 · 设备绑定模型+状态跟随 + 默认选项 + 新增 128 个模型

- **设备模型化**（对齐原版 io 物品库机制）：
  - 设备加 `modelId`，绑定实体时 `inferDeviceModel` 按域/名自动推断默认模型（light/switch→按名匹配吊灯/吸顶灯/台灯/落地灯/筒灯/壁灯，默认吸顶灯；climate→空调）
  - `DeviceMarker` 改造：有 modelId 用 `GltfModel` 渲染下载的 GLB 模型，无 modelId 小球兜底；**灯/开关亮时加 pointLight 发光**，点击 group 控制
- **UI 调整**：①「绑定」按钮从 BottomBar 移到 Editor 工具栏（「绑定设备」）；②BottomBar「设备N」从死状态文字改为按钮，点开 `DeviceList` 弹窗列出已绑定设备（名称+状态+切换按钮）
- **默认选项**：设置面板（原「设置·背景」改「设置」）加「默认选项」区块——默认旋转方向/速度/画质/投影/全屏/沉浸，`saveDefault` 存 settings，启动时加载（含默认全屏自动 requestFullscreen）
- **新增 128 个模型**（poly.pizza，furn_cont3/4/5 脚本）：厨房柜8/烤箱6/油烟机4/水槽5/灶台4/烤面包机3/电脑6/显示器6/笔记本4/键盘5/鼠标5/监控8/烟感器4/门铃4/路由器3/无人机4/耳机5/榨汁机4/空气炸锅3/电饭煲3/热水器6/空调6/智能音箱6/门8/窗8。总计 **347 个模型 26MB**
- **⚠️ 教训**：①设备用模型显示=数据加 modelId + 渲染时 getCatalogItem 找 GLB + 灯亮加光源，比程序化建模简单得多；②下载脚本用 sleep(0.2) 节流防限流，国外模型库不翻墙下载慢但稳定（0 失败）；③默认选项存 settings 复用 api.saveSettings，启动加载同一份
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-13 23:23 · 修 emptyOutDir 清空模型 + 设备分类筛选

- **🔴 大坑：`vite.config.js` 的 `emptyOutDir: true` 会清空 outDir(webui) 整个目录，把 `models/` 里的模型文件也删了**。之前 rsync 同步 347 个模型后，一 build 就被删回 219 个，导致「新模型在家具里没有」
  - **修复**：`emptyOutDir: false`（不再清空 outDir，旧 hash 的 assets 残留可接受，模型文件保留）
  - **教训**：vite build 的 `emptyOutDir` 清空的是**整个 outDir**，不只 assets——outDir 里放了模型/静态资源时，绝不能开 emptyOutDir
  - **排查路径**：发现「新模型没显示」→ 查 rootfs 磁盘只有 219 个 glb → git log 对比发现 commit 50f5803 加了 128 个、下一个 commit 9190c62 又删了 → 定位到 build 的 emptyOutDir
- **设备分类筛选修复**：Editor「设备」按钮点出的分类（灯光/开关/空调…）之前只开抽屉不传分类；加 `bindCat` 字段，点分类 `setState({bindOpen:true, bindCat:c})`，BindDrawer 用 `bindCat` 初始化筛选
- **验证**：SSH 进加载项容器 `ls models/*.glb | wc -l` = 347，厨房柜_219.glb 等新模型已就位 ✓
- **⚠️ 铁律**：以后改完 build 相关配置，必须验证「build 后 models 数不变」——这是数据（模型文件）被 build 静默删除的坑

## 2026-08-13 23:45 · 大批功能：自选模型/存档/墙线拉伸/布局/模型缩放

- **实体自选模型**（用户强调"不是帮我选，是我自己选"）：设备属性面板加「模型」下拉（按分类 optgroup，从 modelCatalog 选），`patchDevice` 改 modelId；自动推断只作为绑定时默认，用户可改
- **保存查看（存档）**：后端加 `/api/backups`（列存档）+ `/api/backup`（创建带时间戳副本，存 `BACKUP_DIR=/share/ai_3d_home/backups`）+ `/api/backup/restore`（恢复）；前端「保存」= 保存+创建存档，「最近备份」打开存档列表（时间+恢复）。回答了用户"保存完东西在哪"——存档列表可见可恢复
- **2D 墙线拉伸**：select 工具点墙线选中 → 显示两端 `plan-wall-handle` 圆点，拖动端点 `wallDragRef` 改 start/end → `recomputeRooms` 重算房间
- **2D 房间改名**：select 工具点房间 → 房间属性面板改 `name`
- **布局**：家具弹窗 `.plan-props` 240→320px + max-height 滚动；编辑器底部「−＋居中/复制当前层/删除楼层」移到顶部工具栏
- **清理**：移除「参考底图」（没实现）、「导入/导出 JSON」（实现不了，被存档功能替代）；「重置当前户型/清空图纸」改名「清空当前层/清空全部」消歧义
- **模型 2D/3D 尺寸方向匹配**：`GltfModel` 接收 w/d/h——①方向对齐（模型竖着但目标横着，或反之，`rotation.y=π/2`）；②等比缩放 `min(w/sz.x, d/sz.z, h/sz.y)` 放进目标盒子不拉伸；调用处传 CATEGORY_DIMS 的 w/d/h
- **验证**：build 通过 ✓；build 后 models 仍 347（emptyOutDir:false 生效）✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①GLB 模型方向/尺寸和 2D 占地不匹配，根治=加载后 Box3 测实际尺寸→方向对齐旋转+等比缩放放进目标盒子；②「保存查看」=后端存时间戳副本+前端列表恢复，比只覆盖式保存好

## 2026-08-14 07:30 · 导出 JSON/PNG/SVG + 导入底图/删除底图

- **导出三种格式**（Editor 顶部按钮）：
  - `导出 JSON`：Blob 下载户型数据（备份/迁移/分享，能导回来）
  - `导出 SVG`：`document.querySelector('.plan-editor')` 克隆 + XMLSerializer 序列化，下载矢量平面图
  - `导出 PNG`：SVG 序列化 → Image → canvas → toDataURL，下载 2 倍分辨率位图
- **导入底图**（原「参考底图」）：`importPlanImage` 选图片 → FileReader 读 data URL → 存 `store.planImage`；PlanEditor 网格上方渲染 `<image>` 铺满 viewBox、半透明（`planImageOpacity` 0.4）、`pointer-events:none` 不可交互，用户照着画墙
- **删除底图**：`removePlanImage` 清空 planImage，按钮只在有底图时显示
- **验证**：build 通过 ✓；models 仍 347 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①SVG 导出直接 `XMLSerializer().serializeToString(cloneNode)`，PNG 导出走 SVG→Image→canvas→toDataURL，都要先 `setAttribute('xmlns', ...)`；②底图参考用 `<image>` 半透明 + pointer-events:none，不干扰画墙交互

## 2026-08-14 07:39 · 底图缩放 + 设备模型缩略图 + 编辑器默认选项

- **底图缩放**：store 加 `planImageScale`；PlanEditor 底图渲染 `imgW=vbW×scale, imgH=vbH×scale` 中心锚定；Editor「底图＋/底图−」按钮 ×/÷1.25
- **设备模型缩略图**：设备属性面板模型选择从 `<select>` 改成缩略图网格——按分类分组标题 + `thumbUrl(m.thumb)` 36×36 缩略图，点缩略图选，选中高亮边框，「无（圆球）」置顶
- **编辑器默认选项**：设置面板加「编辑器默认」区块——默认吸附/标签/名字(开/关)、默认视图(2D/3D)、默认缩放比例(60~150%)；存 settings（`snap/showLabels/showFurnitureLabels/defaultView2d/furnitureScale`），App 启动加载到 store
- **验证**：build 通过 ✓；models 347 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 07:52 · 修家具反复不显示（public/models 覆盖）+ 默认选项移入 2D 编辑器 + 设备数量去重

- **🔴🔴 大坑：`web/public/models/` 目录是 vite 的 publicDir，build 时会被原样复制到 outDir，覆盖 rootfs 里的模型**。public/models 是旧的 219 个，每次 `npm run build` 都把 rootfs 的 347 个覆盖回 219 → 「家具反复显示不出来」的真正根因（之前修 manifest、修 emptyOutDir 都没解决，因为真凶是 public/ 覆盖）
  - **修复**：把 `public/models/` 也 rsync 成 347（和 rootfs 一致），build 复制的就是 347
  - **教训**：vite 项目里 `public/` 目录 = 构建时原样复制到输出目录。模型/静态资源如果放 public/，改 rootfs 没用，必须改 public/（或别放 public/）。排查「改了半天不生效」先想 public/ 是否在覆盖
- **默认选项移入 2D 编辑器**：从主界面「设置」面板移除「编辑器默认」，改到 Editor 顶部「默认」按钮 → 弹窗（吸附/标签/名字/默认视图 2D3D/缩放 60~150%），`saveDefault` 存 settings
- **设备数量去重**：BottomBar deviceCount 从「设备对象数」改成 `new Set(entity_id).size`——同一个实体绑多个模型只算 1 个
- **验证**：build 后 models 仍 347 ✓；容器里厨房柜模型在 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 08:07 · 修 2D 导出 + 加 3D 图片/视频导出

- **修 2D 导出黑白问题**：导出的 SVG/PNG 之前是黑白的，因为样式在 CSS 类里（`.plan-*`）不在 SVG 内联。修复：`getPlanSvg()` 克隆 SVG 后，遍历 `document.styleSheets` 收集 `.plan-*` 规则塞进 `<style>` 标签 + 设 width/height（getBoundingClientRect），导出才有颜色
- **3D 导出（BottomBar 加两个按钮）**：
  - `📷 3D图`：`document.querySelector('.canvas-wrap canvas').toDataURL('image/png')` 截图（WebGL canvas 需 preserveDrawingBuffer:true，Viewer 已配）
  - `🎬 3D视频`：`canvas.captureStream(30)` + `MediaRecorder` 录 6 秒自动旋转，下载 webm
- **导出分开放**：2D 导出（PNG/SVG/JSON）在编辑器（Editor），3D 导出（图片/视频）在底部栏（BottomBar），符合"2D 导出在 2D 里、3D 导出在 3D 里"
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①SVG 导出要用内联 CSS（克隆后塞 `<style>`），否则打开黑白；②WebGL 截图必须 preserveDrawingBuffer:true，否则 toDataURL 空白；③视频录制 canvas.captureStream + MediaRecorder，录制时开 autoRotate 让画面动起来

## 2026-08-14 08:10 · 墙闭合：起点吸附 + 起点标记

- **问题**：画墙最后一段很难闭合——起点不在网格上、肉眼看不到闭合点，最后一段点不准起点
- **修复**：
  - `snap()` 画墙分支加**起点吸附**：鼠标靠近画墙起点（<0.5m）自动吸附到 `draft.pts[0]`，最后一段自然吸到起点闭合
  - 画墙时起点位置渲染**绿色圆点标记**（`.plan-start-marker`），让用户一眼看到闭合点在哪
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：画墙闭合难的本质是「起点是自由点、不在吸附网格上」——只靠网格/端点吸附不够，必须专门对「画墙起点」做吸附 + 视觉标记，用户才知道往哪点

## 2026-08-14 08:17 · 修画墙闭合 + 没闭合却出地面的根因

- **「没闭合却出现地面」根因**：`detectRooms` 把「墙段端点落在相邻墙段上」当连接（共用墙机制的合理设计），所以画墙有微小缺口时，墙段端点落到相邻墙上 → 被桥接成闭环 → 出地面。真正的毛病在画墙端闭合不精准
- **修复画墙闭合（三招）**：
  1. `addWallPoint` 闭合检测从「用 gx/gy（只网格吸附）」改成「用 snap(raw)（含起点吸附）后的点」——之前起点吸附生效了但闭合判断没生效，导致点回起点附近不闭合
  2. 闭合半径 `CLOSE` 0.5 → 0.8，更容易合上
  3. 起点标记在鼠标靠近（<CLOSE）时放大变亮，提示「可以点这里闭合」
- **验证**：node 测 detectRooms（0.1/0.3 间隙都会桥接出 1 房间，L形3段=0，穿过=0）；build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：闭合判断必须用「吸附后的最终坐标」，不能用手工再算一遍网格吸附——吸附逻辑和判断逻辑要共用同一套 snap 结果，否则两者不一致（吸附了但判断没吸附）

## 2026-08-14 10:00 · 空调/热水器改程序化建模 + 移除错误下载模型

- **问题**：poly.pizza 是国外免费模型库，**没有中国常见家电**（分体空调、储水式热水器、燃气灶）。搜 "water heater" 返回洗衣机/烘干机/灶台/冰箱（分类完全错误），"air conditioner" 混入 Air Vent/Window fan
- **方案**：空调、热水器改用**程序化建模**（像真的几何体）：
  - `空调`：白色扁机身 + 出风口格栅（3 条横线）+ 绿色指示灯，placement wall
  - `热水器`：横卧白色圆筒 + 端盖 + 底部进出水管，placement wall
  - 加到 FURNITURE_LIB + FurnitureModel case，用户从「内置家具」选
- **清理**：移除「热水器」分类 6 个错误下载模型（实际是 Washer/Dryer/Stove/Fridge/Air conditioner，且洗衣机/灶台/冰箱分类已各有 4 个，重复）
- **验证**：build 通过 ✓；341 个模型（347-6）✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：免费 GLB 模型库（poly.pizza）的搜索结果标题千篇一律（都是 "Air conditioner"），且**搜索词模糊匹配**会返回大量无关模型——下载前要看 title 过滤，搜不到目标（热水器）就改用程序化建模，别硬塞无关模型

## 2026-08-14 10:13 · 空调筛选重下 + 模型删除按钮

- **空调筛选重下**：搜索 "air conditioner" 时用标题过滤（排除 vent/fan/purifier/heater），下载 8 个真正的空调替换旧的（旧的含 Air Vent 通风口）
- **模型删除按钮（用户自己删）**：家具选择器每个下载模型缩略图右上角加「✕」按钮，点击 `hideModel` 把模型 name 加到 `settings.hiddenModels` 隐藏；家具选择器渲染时过滤 hiddenModels。**用户明确「不要的模型我自己删」**——以后不擅自移除模型，给删除按钮让用户决定
- **教训**：①我擅自移除热水器/空调旧模型太激进，用户要的是「给删除按钮自己删」——涉及用户数据的清理操作，先给入口让用户决定，别替用户删；②孤儿文件清理用 manifest 对照删 glb/thumbs
- **验证**：build 通过 ✓；343 个模型 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 10:20 · 底部栏整理：分享/画质合并菜单 + 视频 mp4

- **编辑按钮加 ✏️ 图标**：`{editing ? '退出编辑' : '✏️ 编辑'}`
- **3D图/3D视频 合并「📤 分享」**：点击弹出菜单（导出 3D 图片 / 录制 3D 视频），`.bb-menu` 绝对定位菜单
- **3D 视频改 mp4**：`MediaRecorder.isTypeSupported('video/mp4')` 优先 mp4，不支持回退 webm（之前 webm 用户看不了）
- **极致/投影 合并「🎨 画质」**：菜单里流畅/均衡/高清/极致 + 投影开关，删掉 cycleQuality
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①MediaRecorder 录 mp4 要 `isTypeSupported` 判断，Safari 支持 mp4、Chrome 支持 webm/mp4 视版本，回退保证兼容；②底部栏按钮多了要用下拉菜单收敛（分享、画质），`position:relative` 包一层 + 绝对定位菜单

## 2026-08-14 11:28 · 重新下载 60 个家具（标题过滤筛选）

- **背景**：用户反馈旧家具质量差、搜索词错配（搜热水器冒出洗衣机）。找免费+不用注册的源，Pixabay/Creazilla 403 反爬，ToxSam/Quaternius 是游戏场景非家具，最终仍是 poly.pizza 可稳定抓取
- **改进下载**：`furn_refresh.py` 精确搜索词 + **标题过滤**（每个分类标题必须含关键词才下载，如沙发须含 sofa/couch，冰箱须含 refrigerator/fridge），重新下 60 个核心家具（沙发/床/餐桌/书桌/椅/衣柜/电视/冰箱/洗衣机）
- **⚠️ 拒绝绕过付费**：用户要求「绕过付费机制抓付费模型」，已拒绝（盗版/侵权），说明只能做免费源/CC0/购买后集成
- **验证**：403 个模型（343+60）✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①标题过滤仍不完美（"wash" 会匹配到 Dishwasher/Washing Line），但比无过滤强，配删除按钮让用户自己删；②大 commit（60 模型）push 时网络抖动反复失败，重试 6-10 次最终成功

## 2026-08-14 12:04 · 缩略图预览 + 截图数量/视频秒数可调

- **缩略图点击预览**：家具选择器点缩略图不再直接放置，改为弹预览弹窗（放大缩略图 + 尺寸信息 + 「使用此模型」按钮 + 关闭）
- **截图数量可调**：分享菜单「导出 3D 图片」分 1/4/8 张——多张时自动旋转、每 0.9 秒截一张不同角度
- **视频秒数可调**：分享菜单「录制 3D 视频」分 3/6/10 秒
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①多角度截图=自动旋转中定时 toDataURL，旋转起来才能截到不同角度；②菜单带分组标题（.bb-menu-title）让选项更清晰

## 2026-08-14 12:53 · 模型选择器优化

- **「家具」按钮改名「模型」**；选择器顶部标注「共 N 个模型」（内置 + 下载模型过滤 hiddenModels 后）
- **内置家具也加预览**：点击内置家具 → 预览弹窗显示色块（无缩略图）+ 尺寸 + 放置按钮
- **缩略图去掉删除按钮**，删除移到预览弹窗里；预览弹窗「使用此模型」改名「放置」，加「删除」按钮（仅下载模型，内置不可删）
- **预览图放大**：maxHeight 260→320，内置家具色块 110×110
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 13:15 · 模型 3D 预览 + 内置可删 + 删除确认 + 尺寸入弹窗

- **模型 3D 预览**：新建 `ModelPreview.jsx`（独立 Canvas + autoRotate OrbitControls + 光照），内置家具用 `FurnitureModel`、下载模型用 `GltfModel` 渲染，可拖拽旋转查看；Scene.jsx 导出 `GltfModel`/`FurnitureModel`
- **内置家具也支持删除**：`FURNITURE_LIB.filter` 过滤 hiddenModels；预览弹窗删除按钮去掉 builtin 条件
- **删除二次确认**：删除按钮 `confirm('确认删除…')`
- **尺寸移入预览弹窗**：选择器顶部全局「尺寸」移除，预览弹窗加「缩放 60~150%」
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：①预览 3D 模型要独立 Canvas + 自建 OrbitControls（不复用 store 依赖的 Controls 组件）；②内置家具隐藏用同一个 hiddenModels（type 是中文名如「沙发」，与下载模型文件名共存于同一列表）

## 2026-08-14 13:37 · 预览默认图片+3D切换 + 内置家具图标缩略图

- **预览默认放大图**：点模型默认显示放大缩略图（下载模型）/色块（内置），弹窗加「🧊 3D预览」按钮，点击才切 3D 渲染（`show3d` 状态，关闭时 `closePreview` 重置）
- **内置家具图标缩略图**：内置家具（程序化建模无图片）用 emoji 图标代替色块（🛋️🛏️🍽️🪑🚪🗄️📺💡❄️♨️…），`FURNITURE_ICONS` 映射
- **⚠️ 教训**：`replace_all` 替换 `setPreviewModel(null)` 时误伤了 `closePreview` 定义本身（变成 `closePreview(){closePreview()}` 无限递归）——replace_all 要检查是否波及到新定义的目标字符串，改完 grep 验证
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 14:11 · 内置家具直接3D + 2D编辑器右侧信息栏

- **内置家具点开直接 3D**：内置家具（无图片）点击预览时 `setShow3d(true)` 直接进 3D，下载模型仍默认放大图 + 点「3D预览」切换
- **2D 编辑器右侧信息栏**（`.editor-info`）：显示 楼层（当前/总）、面积（`polygonArea` 累加 ㎡）、房间数、家具数、设备数（entity_id 去重）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 14:25 · 弹窗放大 + 修楼层数 bug

- **弹窗放大**：预览弹窗 width 400→560，3D 预览区 height 280→400，图片 maxHeight 280→400
- **修楼层数 bug**：信息栏「楼层」误用了 `currentFloor`（import 的函数名）做 `currentFloor + 1`，函数+1 算出字符串；改成订阅 `currentFloorIdx = useStore(s => s.currentFloor)`，显示 `currentFloorIdx + 1 / floors.length`
- **⚠️ 教训**：import 的函数名和 store 的数值同名（`currentFloor` 既是 store 字段又是 store 导出的函数），在组件里混用会踩坑——store 字段要用 `useStore` 订阅取数值，不能直接用同名函数

## 2026-08-14 14:36 · 楼层切换 + 点击空白关闭弹窗

- **楼层切换**：信息栏「楼层」加 ◀ ▶ 按钮（`prevFloor`/`nextFloor`，边界保护），解决「无法切换楼层」
- **点击空白处关闭弹窗**：Editor 模型选择器/设备分类 + BottomBar 分享/画质菜单，加 `document.addEventListener('mousedown')` 监听，点击弹窗外（不在 `.furn-picker`/`.bb-menu` 内）关闭
- **⚠️ 教训**：点击外部关闭要 `e.target.closest('.弹窗类')` 判断点击是否在弹窗内，且要排除触发按钮（否则点按钮变成「开又立刻关」）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 15:02 · 楼层管理面板 + 房间改名合并到信息栏

- **楼层管理面板**：信息栏加「楼层管理」按钮 → 弹窗（楼层列表点击切换 + 新建空白楼层 `newFloor` / 复制当前层 / 清空当前层 / 删除楼层）；信息栏去掉 ◀ ▶ 切换按钮
- **房间改名合并到信息栏**：房间属性面板（plan-props）和右侧信息栏位置冲突，把「当前房间名」输入框整合进信息栏（选中房间时显示），移除 PlanEditor 的 selRoom/patchRoom 面板
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：两个常驻面板（信息栏/属性面板）都用 `position:absolute` 右上角会重叠——改名这类轻量编辑整合进信息栏，重量级编辑（家具/门窗/设备）保留独立属性面板

## 2026-08-14 15:14 · 撤销功能 + 顶部栏整理 + 房间信息

- **撤销(undo)**：store 加 `undoStack`（setState 修改 project 时深拷贝快照，限 50 步）+ `undo()` 恢复；顶部栏「↩️ 撤销」按钮
- **顶部栏整理**：去掉「全屋」「复制当前层」「删除楼层」「清空当前层」（和楼层管理重复）；「楼层管理」移到顶部栏；「最近备份/导出图/导出SVG/导出JSON」合并成「备份」下拉菜单；加「撤销」
- **房间信息**：信息栏选中房间时显示「当前房间」卡片（名字输入 + 面积 ㎡），放在户型信息下面
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：撤销靠 setState 里拦截 `next.project` 变化存快照，undo 里直接赋 state（不经过 setState 保存逻辑）避免撤销又触发存快照

## 2026-08-14 15:28 · 楼层/墙高度 + 设备模型分组 + 标定功能

- **楼层高度调整**：楼层管理面板每层加高度输入（2~6m），改 `floor.height`
- **墙高度调整**：墙 3D 渲染用 `w.height || floor.height`，信息栏选中墙显示「当前墙高」输入
- **设备模型按类型分组**：设备面板模型选择按「灯光/摄像机/安防」分组（DEVICE_MODEL_GROUPS 映射 label）；补「家电/设备」内置组（空调/热水器/窗帘/传感器 emoji）
- **补窗帘/传感器程序化模型**：FURNITURE_LIB + FurnitureModel 加「窗帘」（横杆+竖条布）、「传感器」（圆盘+指示灯）
- **标定功能（对齐原版）**：底图比例尺标定——导入底图后点「标定」，点底图选起点/终点，输入实际长度（默认3米），`新缩放 = 当前缩放 × 实际长度 / 两点距离`
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓
- **⚠️ 教训**：标定核心公式 `planImageScale × realLen / dist`（两点世界距离 vs 实际米数，等比换算缩放）

## 2026-08-14 15:35 · 补齐原版缺失功能：日夜/尺寸/地板颜色/灯带

- **日夜切换**：BottomBar 加「☀️ 日间/🌙 夜间」按钮（night 状态之前只有 store 没有 UI），去掉 disabled 的「璃玻璃」占位
- **尺寸标注**：store 加 `showDimensions`，2D 墙渲染显示墙长度（`.plan-dim` 蓝字白描边），Editor「尺寸」开关
- **地板颜色**：选中房间信息栏加 8 色地板调色板（FLOOR_COLORS），改 `room.color`
- **灯带**：FURNITURE_LIB + FurnitureModel 加「灯带」（细长发光条 emissive），BUILTIN_DEVICES 加 ✨
- **⚠️ 仍缺（原版有，复杂暂未做）**：Area 绑定建议（自动建议设备绑房间）、视觉风格切换（cartoon/cream）、灯带多端点交互
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 16:33 · 修撤销整没户型 + 备份菜单

- **撤销把户型整没（根因）**：setState 修改 project 时存快照，而「启动加载 project」也走 setState → undoStack 第一个快照是「空户型」→ 撤销到底恢复成空。修复：加 `loadProject()`（直接赋值不存快照），启动加载/导入JSON/恢复备份都用它
- **备份菜单**：backMenuOpen 没纳入「点击外部关闭」逻辑，补上（点击 .bb-menu 外关闭）
- **⚠️ 教训**：撤销快照要区分「用户操作」和「初始化加载」——初始化加载不能用会存快照的 setState，要用专门的不存快照函数，否则撤销到底恢复到初始空态

## 2026-08-14 16:49 · 恢复丢失数据 + 修备份菜单

- **数据"丢"真相**：撤销 bug 把 project 清成空（34 字节 `{"floors":[]}`），自动保存写进了 project.json；备份文件其实还在（4 个）。SSH `cp 最新备份 project.json` 恢复（34→20530 字节）
- **备份菜单点不开**：`.bb-menu` 的 CSS 是 `bottom:40px`（给底部栏用的），顶部栏里的备份菜单用了同一样式 → 菜单跑到按钮上方屏幕外。修复：顶部栏备份菜单加 `top:32,bottom:auto`
- **⚠️ 教训**：①撤销 bug 会把空快照写回并自动保存覆盖 project.json——撤销快照必须排除初始化/加载；②`.bb-menu` 定位假设在屏幕底部，顶部栏复用要覆盖 top/bottom；③「数据丢了」先 SSH 看文件，别慌，备份都在 /share 里

## 2026-08-14 17:21 · 项目/房间墙面板/天气/墙厚度颜色/重叠墙去重

- **项目按钮**：顶部栏「项目」→ 弹窗（项目名输入 + 「新建项目」prompt 命名 → 空项目 loadProject）
- **房间/墙独立面板**：选中房间弹房间面板（名字/面积/颜色）、选中墙弹墙面板（高度/厚度/颜色），右侧信息栏改成「未选中时才显示户型信息」
- **左上角天气温度**：status-tl 从 haEntities 找 weather 实体 + 温度 sensor，显示天气图标+状态+温度
- **墙厚度/颜色**：墙面板加厚度（0.05~0.5m）+ 8 色墙颜色，3D 渲染用 `w.thickness`/`w.color`
- **重叠墙 3D 去重**：3D 墙渲染用 `wallKey` 去重（相同位置只画一层，避免透明墙重叠变厚变暗）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 17:33 · 工具设置面板（点工具右侧弹默认参数）

- **需求**：编辑界面点左侧栏「墙体/门/窗」工具，右侧弹出该工具的设置面板，直接改默认参数
- **实现**：store 加 wallH/wallThick/wallColor/doorStyle/doorColor/doorSwing/windowStyle 默认值；PlanEditor 加工具设置面板（tool 是 wall/door/window 且未选中时显示）；画墙/放门/放窗时用这些默认值（墙段带 height/thickness/color，门带 doorStyle/color/swing，窗带 windowStyle）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 17:53 · 户型信息收起 + 备份并入项目按钮

- **户型信息面板收起**：右侧「户型信息」面板（`.editor-info`）原来只在 `!selected` 时显示，点「墙体/门/窗」工具时和工具设置面板（同为右上角 top:60 right:14）重叠。改成 `!selected && !['wall','door','window'].includes(tool)`——点这些工具弹出设置框时户型信息自动收回，点空白处取消选中后自动显示
- **备份并入项目按钮**：顶部去掉「备份」按钮，把 最近项目(存档列表)/导出图/导出SVG/导出JSON 都并入「项目」弹窗。项目弹窗改为：项目名 + 最近项目(每条 打开/删除) + 新建项目 + 三个导出按钮。语义对齐用户：项目=存档快照，打开项目=恢复存档，删除项目=删除存档
- **后端加删除存档接口**：server.py 加 `POST /api/backup/delete`（body {name}，`os.path.basename` 收口 + `endswith('.json')` 校验防路径穿越，和 restore 同款）；api.js 加 `backupDelete()`
- **⚠️ 教训**：①两个常驻面板同位置（户型信息 160px 宽 / 工具设置 320px 宽，都在 top:60px right:14px）会重叠，显示条件里要把工具态一并排除，不能只看 `!selected`；②删存档接口必须 basename + .json 校验防穿越，不能拿 name 直接拼路径
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 18:11 · 存档命名(项目名_日期) + 新建项目先存档 + 右键取消/双击右键移动

- **存档命名 项目名_日期**：后端 `/api/backup` 从 project.name 取名，命名 `项目名_YYYYMMDD_HHMMSS.json`（没名字就只日期），去掉旧 `backup_` 前缀；`/` `\` 空格替换成 `_` 防路径问题。前端「最近项目」列表显示 `backupLabel`（去 .json 和旧 backup_ 前缀）
- **新建项目先存档**：之前「新建项目」直接 `loadProject` 空项目把当前项目丢了。现在点新建 → 先 `api.saveProject(当前)` + `createBackup()` 存成「项目名_日期」快照 → 再开空项目，toast 提示「之前的项目已存为最近项目」
- **右键单击=取消 / 双击右键=移动**：PlanEditor `handleFloorDown` button===2 分支，用 `rightClickRef` 时间戳区分（<300ms 算双击）。单击右键 = `tool→select` + `selected:null` + 取消画墙草稿 + 退出标定（覆盖选中/移动/墙体/门窗/家具/设备的放置/删除所有取消场景）；双击右键 = `tool→move`
- **⚠️ 教训**：①「新建项目」这种会覆盖当前项目的操作，必须先存当前为快照再覆盖，否则用户的数据被静默丢弃；②双击/单击右键冲突靠时间戳区分，第一次单击会先执行「取消」，第二次（<300ms）才切移动——结果正确（移动态+无选中）；③备份文件名带项目名时，`/` 必须替换，否则 `os.path.join`/`basename` 会错位
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 18:29 · 修复相邻户型共用墙闭合识别不到

- **现象**：2D 画户型，第 1 个房间 4 面墙能闭合，第 2 个相邻房间画 3 面墙（第 4 面靠共用墙）却不出房间，第 3 个同理。用户期望「画 3 面墙 + 共用墙重合就闭合」
- **根因（关键）**：算法 `detectRooms`/`recomputeRooms` 本身没问题——用真实数据（SSH 拉 /share 的 project.json，9 墙 1 房间）单独跑 `recomputeRooms` 能正确识别出 2 个房间。问题是 **`addWallPoint` 每加一面墙根本没调用 `recomputeRooms`**，只有点回起点/按 Enter 触发 `closeDraft` 才识别。用户画相邻户型只画 3 面墙（不点回起点），房间就永远不识别
- **修复**：`addWallPoint` 每加一面墙就增量 `recomputeRooms`；若房间数增加了（墙已靠已有墙闭合出新房间），**自动结束本次画墙**（不额外加闭合墙，避免和共用墙重复）。顺手修 `recomputeRooms` 的 `color` 永远重置为 `FLOOR_PALETTE[0]` 的 bug（改成保留旧房间颜色）
- **⚠️ 教训**：①「画了墙但不出房间」先分清是「算法识别不到」还是「根本没调用识别」——用真实数据单独跑 `recomputeRooms` 能定位（这次算法是对的，是调用时机漏了）；②画墙的房间识别应该在**每加一面墙时增量跑**，而不是只在闭合时跑一次，否则共用墙/线重合这种「不点回起点的闭合」永远漏识别；③`recomputeRooms` 匹配旧房间时要保留 color，别每次重算都重置用户设的颜色
- **验证**：真实数据 detectRooms 跑出 2 房间 ✓；build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 18:39 · 分割墙识别 + 碰墙蓝色提示

- **分割墙立即识别**：在已有房间中间画一条分割墙（两端顶在房间两条墙上），之前识别不到。根因是**画墙吸附只有「端点吸附」没有「墙中点吸附」**——分割墙两端落在墙中点（不是角点），网格吸附会把点从墙上挪开，导致分割墙连不上房间边、detectRooms 识别不出两个房间
- **墙吸附（含中点）**：`wallSnapPoint` 用 `pointToSeg` 找光标到已有墙的最近点（阈值 0.3m，含中点/端点），排除本次草稿已画的墙（避免光标吸回刚画的墙）。吸附优先级：起点闭合吸附 > 墙吸附 > 网格吸附 > 轴向吸附。分割墙两端因此能精准落在房间边上，detectRooms 立刻识别出两个房间（`addWallPoint` 的增量识别 + 自动闭合已在上一轮生效）
- **碰墙蓝色提示**：画墙时光标碰到已有墙，光标点变蓝色（`.plan-cursor-wall` 蓝点+白描边），没碰墙是灰点（`.plan-cursor` 改为灰）。store 加 `cursorWall` 状态，`handleMove` 里 `wallSnapPoint` 判断
- **⚠️ 教训**：①分割墙/共用墙连不上，根因常是「只吸附端点、不吸附墙中点」——墙吸附要用 `pointToSeg` 求到线段最近点，不能只比对端点；②墙吸附优先级要高于网格吸附，否则网格先把点从墙上挪走；③墙吸附要排除草稿自己的墙，否则光标会吸回刚画的那段墙
- **验证**：detectRooms 分割墙（竖/横）都识别 2 房间 ✓；build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 18:46 · 画墙水平/垂直提示 + 选中墙显示长度可改 + 起点绿终点蓝

- **水平/垂直提示**：画墙时当前段被轴向吸附成水平/垂直后，在预览线中点显示「水平」/「垂直」小字（`.plan-align-hint` 绿色白描边）。判断：`|dy|<0.01 && |dx|>0.01` = 水平，`|dx|<0.01 && |dy|>0.01` = 垂直
- **选中墙显示长度并可改**：墙属性面板加「长度」行（数字输入，`Math.hypot(end-start)` 实时显示）。改长度 = `setWallLength`：**起点固定，终点沿墙方向移动**（`end = start + 方向单位向量 × 新长度`），加长向终点方向延伸、缩短向起点方向收，改完 `recomputeRooms` 重算房间
- **起点绿/终点蓝**：选中墙的两个端点手柄 `.plan-wall-handle` 拆成 `.start`（绿 #2ecc71）/`.end`（蓝 #2f7fe0），一眼看清哪头是起点哪头是终点
- **⚠️ 教训**：改墙长度 = 固定起点、终点沿方向向量缩放（`end = start + dir * L`），不要动起点；改完要 `recomputeRooms`（墙端点变了会影响房间形状）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 19:09 · 墙体透明度滑块 + 墙方向显示 + 两墙约束(垂直/共线/水平) + 拖动轴向停顿

- **墙体透明度（不透明度滑块）**：store 加 `wallOpacity`(默认100)、墙加 `opacity` 字段。墙工具设置面板 + 选中墙面板都加「不透明度」range 滑块(10~100%)。2D 墙 `strokeOpacity=opacity/100`；3D 墙 `opacity = w.opacity!=null ? w.opacity/100 : th.wallOpacity`（默认不写 opacity 字段，3D 保持原版 0.197 半透明；只有用户改过才覆盖）。墙创建时 `wallOpacity!==100` 才写 opacity 字段
- **墙方向显示**：选中墙面板加「方向」行（水平/竖直/斜线），按 `|dy|<0.01` / `|dx|<0.01` 判断
- **两墙约束（垂直/共线/水平）**：墙改成多选（store `wallSel` 最多 2 条，点墙 toggle、点空白/右键/点非墙清空）。选 2 条墙时弹「两墙约束」面板，显示 `墙1:水平 · 墙2:竖直 · 关系:垂直`（点积≈0=垂直，叉积≈0=共线/平行），三个按钮作用在第 2 面墙（保持第 1 面不动）：垂直=方向取第1面墙法向、共线=起点投影到第1面墙直线+方向对齐、水平=变水平。改完 `recomputeRooms`
- **拖动轴向停顿**：拖墙端点时加 `snapAxis`（相对另一端锚点的水平/垂直吸附，阈值 |dy/dx|<0.18 约10°），水平/竖直有「停顿/吸住」感。之前拖动只在 select 工具下走网格吸附、没有轴向吸附
- **⚠️ 教训**：①墙从单选改多选，要把 `isSel('wall')`/`selWall` 全部从 `selected` 切到 `wallSel` 数组，否则面板和高亮不一致；②约束操作固定「第1面墙不动、动第2面墙」最直观；③透明度默认不写字段（用 theme 的 wallOpacity 0.197），只有用户主动改才覆盖，避免把 3D 原本的半透明墙默认变实心
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 19:22 · 墙方向/角度/起终点编辑 + Ctrl多选 + 去平移 + 轴向吸附改5°

- **墙面板增强（以起点为基准）**：选中墙面板加 起点(X/Y)/终点(X/Y) 数字输入（`setWallStart`/`setWallEnd`，改完 recomputeRooms）、「⇅ 调换」按钮（`swapWall` 交换起终点）、方向行加「水平/竖直」按钮（`setWallDir`，保持起点+长度+朝向符号）、「角度」数字输入（`setWallAngle`，`end = start + (cos,sin)*len`，起点固定按角度旋转）
- **Ctrl 多选墙**：墙点击改成——普通点击单选（`wallSel=[id]` 替换），Ctrl/Cmd 点击走 `toggleWallSel` 多选（最多 2 条）。之前是普通点击就多选，改成显式 Ctrl 多选更符合习惯
- **去平移按钮**：左侧快捷栏删掉「平移」（`LEFT_TOOLS` 移除 pan），中键拖动画布平移仍保留（`handleFloorDown` 的 button===1 分支）
- **轴向吸附改 5°**：水平/竖直吸附阈值 `0.18`(≈10°) 改成 `0.0875`(≈5°)，画墙和拖端点两处都改。用户嫌 10° 太大，容易误吸
- **⚠️ 教训**：①方向修改统一「以起点为基准、保持长度」，水平/竖直要保留朝向符号（`dx>=0?1:-1`），否则会翻转墙；②角度用 `end = start + (cosθ,sinθ)*len`，长度不变；③多选要么普通点击多选、要么 Ctrl 多选，别混着让用户懵
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 19:29 · 户型信息加墙数/几何关系统计 + 左侧多选按钮

- **户型信息统计**：`.editor-info` 加「墙 N 条」+「水平 X / 竖直 Y / 斜线 Z」（几何关系个数），按墙方向 `|dy|<0.01` 水平、`|dx|<0.01` 竖直、其余斜线统计
- **左侧多选按钮**：store 加 `multiSelect`，左侧快捷栏底部加「多选」开关按钮（active 高亮）。开启后点墙直接多选（`toggleWallSel`），不用按住 Ctrl；关闭时点墙单选。Ctrl/Cmd 点击仍始终多选
- **⚠️ 教训**：①「多选」是模式开关不是工具，单独一个 boolean（multiSelect），点墙逻辑里 `e.ctrlKey||e.metaKey||multiSelect` 一起判断；②统计几何关系（水平/竖直/斜线）用墙方向的 `|dy|<0.01`/`|dx|<0.01` 阈值，和墙面板方向判断一致
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 20:02 · 户型信息分行 + 修多选bug + 背景图多张持久化 + 门窗开关 + 3D墙透明度

- **户型信息分行**：水平/竖直/斜线 从一行挤三个改成各占一行（用户嫌挤，以后统计类尽量一项一行）
- **修多选 bug**：根因是墙的 `onClick` 之前用，而 SVG 的 `onPointerDown`（handleFloorDown 的「点空白取消选中」）会先清掉 wallSel，再轮到 onClick 的多选 → 第二面墙总是丢第一面。改成墙用 `onPointerDown` + `stopPropagation`（挡住 SVG 的清空），多选才生效
- **背景图多张持久化**：后端加 `BG_DIR=backgrounds/`，`POST /api/background` 存成 `bg_时间戳.png` 返回 name，`GET /api/backgrounds` 列表，`GET /api/background/<name>` 取图，`POST /api/background/delete` 删。前端 settings 存「文件名」不再存带 ingress token 的完整 URL（token 每次重装会变→图消失的根因），Viewer 渲染时 `BASE+'api/background/'+name` 现算。设置面板加「已保存的背景图」缩略图列表（点选/✕删）
- **门窗显示开关**：store 加 `showOpenings`，3D 场景门窗渲染加守卫，底部栏「🧱 墙」旁加「🚪 门窗」按钮
- **3D 墙透明度生效**：3D 墙 `opacity` 从 `w.opacity!=null ? w.opacity/100 : th.wallOpacity(0.197)` 改成 `(w.opacity!=null ? w.opacity : 100)/100`——默认 100% 不透明，2D 里拉的透明度在 3D 生效。墙颜色加纯白 `#ffffff`
- **⚠️ 教训**：①「点第二个对象第一个就取消」= 事件顺序问题，`onPointerDown`(清空) 早于 `onClick`(选择)，多选用 `onPointerDown`+stopPropagation 挡住；②存图片/资源要用「文件名」不存带 token 的 URL，token 每次部署变；③用户对「挤」很敏感，统计信息一项一行
- **验证**：build 通过 ✓；server.py 语法 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 20:19 · 裁剪工具 + 修正共线 + 同点 + 角度±90°

- **裁剪工具**：左侧栏加「裁剪」(X 键)。点一面墙，用 `segmentIntersect` 找它和别的墙的最近交点，在交点处切成两段（`target.end=交点` + push 新墙 `交点→原end`），改完 `recomputeRooms`。交点恰好在端点就不裁（本来就是端点）。吸附到交点、切完房间仍闭合
- **修正共线**：之前的共线把第2条线投影到第1条线所在直线（会重合）。改成——第2条线接到第1条线**终点**，沿第1条线方向延长（`b.start=a.end; b.end=a.end+da*lb`），不重合
- **同点**：约束面板加「同点」。找两墙最近的端点对，把第2条线整体平移过去让端点重合
- **角度 ±90°**：墙面板角度行加「+90°」「-90°」按钮，绕起点旋转。+90°(逆时针)`(dx,dy)->(-dy,dx)`；-90°(顺时针)`(dx,dy)->(dy,-dx)`
- **⚠️ 教训**：①裁剪=找交点+在交点处拆两段，交点判断用 `segmentIntersect`，端点不裁；②共线≠投影重合，是「接到终点沿方向延长」；③同点=平移（找最近端点对算 delta），旋转=绕起点改方向
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 20:29 · 裁剪提示 + 设备模型补齐 + 重做 + 墙删除按钮

- **裁剪提示**：裁剪工具加 `cutPoint` 状态，`handleMove` 里 `findCutTarget` 实时算「离光标最近的墙和别的墙的交点」，交点处渲染橙色圆点提示（`.plan-cut-point`）。重构 `cutWall` 拆成 `findCutTarget`（找墙+交点）+ `cutWall(target, pt)`，点击和提示共用一套逻辑
- **设备模型补齐**：程序化建模补 3 个设备——「开关」（墙面面板+翘板）、「感应器」（底座+半球透镜）、「风扇」（电机+4 片扇叶）。加进 FURNITURE_LIB(placement wall/ceiling) + FurnitureModel case + BUILTIN_DEVICES(emoji) + FURNITURE_ICONS + FURNITURE_COLORS
- **重做**：store 加 `redoStack` + `redo()`。`undo` 时把当前 project 压 redoStack，`redo` 弹出恢复并压回 undoStack；`setState` 有新改动时清空 redoStack。顶部「↩️ 撤销」旁加「↪️ 重做」
- **墙删除按钮**：选中墙面板 head 加「删除」按钮（`deleteWall`），原来只能删除工具点删，现在面板直接删
- **⚠️ 教训**：①撤销/重做是「undo 时当前状态压 redo，redo 时当前状态压 undo」，新编辑清空 redo，别用 setState（会触发快照）；②设备模型沿用程序化建模（FBox/mesh 拼几何），不用下载模型，稳定不占空间；③裁剪提示和点击共用 `findCutTarget`，别写两套交点逻辑
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 20:49 · 移植示例设备模型 + 撤销修复 + 约束弹窗冲突 + 右键保留墙

- **移植示例设备模型**：从 JMGLink 原版 bundle（`3d-home-fixed/jmglink-ha-3d-home` 的 minified JS）**完整提取了 49 个设备模型目录**（id/kind/label/placement/defaultHeight），写成 `DEVICE_MODELS`。程序化几何新建 `DeviceModel` 组件（Scene.jsx，按 kind 渲染：灯/开关/窗帘/空调/传感器/感应器/摄像机/门锁/风扇/影音/扫地机/安防面板/通用）。设备选模型面板改用 DEVICE_KINDS 分组显示全部模型；`inferDeviceModel` 改成映射 HA 域→设备模型 id（对齐原版 no 映射 + 名字细分）。⚠️ 原版模型是**程序化代码**不是单独 glb 文件，只能从压缩 JS 里读目录+几何再移植
- **撤销/重做修复（关键）**：之前的 undo 快照在 setState 里 `JSON.stringify(state.project)`，但组件是**就地修改** wall 对象后再调 setState，所以快照拍到的是改后的状态，撤销=空操作。改用 `lastClean` 变量——每次 setState 提交时压入上一次的 clean 快照、更新 lastClean 为本次；undo/redo 里 lastClean 正确交换。loadProject 重置 lastClean+两栈
- **撤销/重做提示**：按钮改纯图标 ↩️/↪️；undo/redo 弹 toast「已撤销（还可撤销 N 步）」「已恢复（还可恢复 N 步）」，到上限提示「已到撤销/恢复上限」
- **约束弹窗冲突**：选两面墙时「两墙约束」面板和「户型信息」面板重叠——户型信息显示条件加 `wallSelIds.length===0`
- **右键取消保留墙**：`cancelDraft` 不再删草稿墙，只结束画墙+重算房间（画了几条线右键后保留，不封闭也留着）
- **⚠️ 教训**：①**就地修改 + setState 会让 undo 快照拍到改后状态**——快照要用「上一次已提交的 deep copy」（lastClean），不能现场 `JSON.stringify(state.project)`；②原版模型是程序化代码，别自己造，先从原版 bundle 提取目录和几何；③两个常驻弹窗同位置要互相排他
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 21:09 · 设备模型统一管理 + 开关状态动效 + 3D网格居中

- **设备模型统一管理**：49 个设备模型加入「模型」选择器（furn-picker 加「设备模型」折叠组，按 kind 分组），点选直接进家具工具放置。FurnitureModel 开头判断 `DEVICE_MODELS.find(id)` → 走 DeviceModel 渲染；家具放置/2D 渲染都补了 deviceModel 分支（placement/defaultHeight/w/d/label）
- **设备开/关状态动效**：`DeviceModel` 加 isOn——吊扇开着时扇叶旋转（`Spinner` 组件 useFrame 转 y）；壁挂空调开着时下方流动半透明风条（`Airflow` 组件 useFrame 位移+淡出），指示灯绿/灰切换；灯/烟感/电视/安防面板等已按 isOn 发光/变色
- **3D 网格居中**：3D 编辑的 `gridHelper` 原来固定在原点，户型画在别处时网格和户型对不上。改成 `position=[camTarget[0], level, camTarget[2]]`（camTarget 是 CameraFocus 算的户型中心）
- **⚠️ 教训**：①程序化设备模型统一放进 DEVICE_MODELS + DeviceModel，家具/设备共用一套渲染和放置逻辑；②开/关状态动效用 useFrame 改 rotation/position/opacity，风扇转扇叶、空调做流动风条；③3D 网格要跟 camera target（户型中心）对齐，别固定原点
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 21:20 · 3D 简单编辑 + 3D 独立背景

- **3D 简单编辑**：3D 视图里现在能点选墙/门窗（select 工具下墙和 Opening 加了 onClick → onSelect）；家具本就可选可拖（move 工具）。新建 `WallProps3D` 组件（3D 里选中墙弹面板：长度/高度/厚度/颜色/不透明度，patch 直接改墙+recomputeRooms），Viewer 在 `editing && selected.type==='wall'` 时渲染
- **3D 独立背景**：store 加 `editorBgImage`/`editorBgMode`（和主界面 bgImage/bgMode 互不影响）。Viewer 的 SceneBackground 在 editing 时用 editorBg*、否则用 bg*。Editor 顶部加「背景」按钮→弹窗：上传背景图（存到 backgrounds/ 复用接口）+「同步主界面背景」（copy bgImage/bgMode）+「清除」
- **⚠️ 教训**：①3D 编辑选墙走 `selected`（单选），2D 走 `wallSel`（多选），两套选择模型分开别混；②3D 独立背景 = 单独 store 字段 + editing 时切换渲染源，上传复用同一套 backgrounds 接口；③3D 面板是 HTML overlay（Canvas 外面渲染）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 21:37 · 空调开状态修复 + 3D 放置/编辑家具设备

- **空调开状态修复（根因）**：DeviceMarker 的 `isOn` 只判断 `state==='on'`，但空调(climate)实体状态是 cool/heat/dry/fan_only/off，永远不是 'on' → 空调永远没动效。改成 climate 域「只要不是 off/unknown/unavailable 就是开」。空调气流/风扇转动效随之生效
- **3D 放置家具**：Scene 加 `PlacePlane`（透明大地平面，家具工具下点击地面用 raycaster+Plane 求世界坐标）+ `placeFurniture`（按 furnitureType/placement/defaultHeight 放家具）。之前 3D 只能选/拖，放不了
- **3D 家具/设备属性面板**：`WallProps3D` 扩展成墙/家具/设备三合一——墙=长度/高度/厚度/颜色/不透明度，家具=高度/旋转/缩放/删除，设备=高度/删除。Viewer 在 `selected.type ∈ [wall,furniture,device]` 时渲染
- **2D 设备高度调节**：设备面板加「高度」输入（改 `pos[1]`）
- **⚠️ 教训**：①不同域的开状态判断不一样——climate 是「非 off 即开」，普通设备是 `==='on'`，别一刀切；②3D 放置 = 透明地面平面 + raycaster 求交点，只有放置工具激活时才渲染平面避免挡点击
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 21:46 · 空调气流动画照搬原版 shader

- **照搬原版气流动画**：之前自造的气流太简陋，用户不满意。这次从 JMGLink 原版 bundle 里**完整提取了 AC 气流的 shader**（`Kh` 顶点着色器：spread/wave/ripple/drop 弯曲平面；`Wh` 片段着色器：sideFade/pulse/vein/coreGlow 流动粒子 + additive 混合）。`FlowPlane`（原版 mn）= shader 平面 + useFrame 更新 uTime；`Airflow`（原版 Nh）= pointLight + 发光条 + 两条气流粒子流（不同 drop/length/opacity/phase/speed/width）。空调开时用 `Airflow color scale=w/0.54`，机身/出风口 emissive 发青蓝光
- **⚠️ 教训**：原版好看的动效都是 shader 做的（additive 混合的流动粒子流），不是几个透明平面能比。要复刻动效就**直接照搬原版 shader**，别自己造
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 22:29 · 视图切换 + 3D右键取消 + 家具点选移动

- **上/前/自定义视图**：store 加 `camViewSignal`(type+n) + `customViews`。Controls 响应信号——top=相机到户型中心正上方往下看，front=正前方平视，custom:<id>=恢复保存的相机 pos+target。BottomBar 加「👁 视图」下拉：上视图/前视图/保存当前视角/已存视角列表(点切换、✕删)。保存视角读 `window.__cam3d`（Controls useFrame 里持续暴露相机 pos/target，不触发 React 重渲染）
- **3D 右键取消**：Viewer 加 `contextmenu` 监听，右键 preventDefault + 清 selected/wallSel（和 2D 右键取消一致）
- **家具点选移动**：store 加 `pickItem`。移动工具下单击家具=拾起（follow 鼠标），再单击=放下；地面也有 PlacePlane 放下。双击家具=切移动工具+拾起。拖动（按住）仍保留
- **⚠️ 教训**：①相机视图切换=改 OrbitControls 的 target+camera.position 再 update()，别只改 camera；②暴露相机位置给 UI 用 window 全局对象（useFrame 里写），别存 store（每帧 setState 会卡）；③「点选移动」用 pickItem 状态，单击 toggle 拾起/放下，onPointerMove 在 isPicked 时跟随
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 22:46 · 右键取消补全 + 默认视图 + 视角持久化 + 移动跟随 + 房顶放置对齐

- **右键取消补全**：3D 右键现在 `tool='select' + 清 selected/wallSel/pickItem`，覆盖移动(取消拾起)、删除(退出删除工具)、放置(退出家具工具)所有取消场景
- **默认视图**：视图下拉加「🏠 默认视图」，恢复原版 iso 视角（CameraFocus 公式 b=dist*0.88，camera=target+(0.86b,0.86b,1.04b)，fov 36）
- **视角持久化**：自定义视角存 localStorage（`ai3d_custom_views`），刷新不丢；保存/删除时 persist
- **移动跟随优化**：拾起家具后改用 useFrame 每帧 `state.raycaster.setFromCamera(state.pointer)` 求交点，不再依赖 onPointerMove——鼠标快速移动也不丢目标（原来家具一离开鼠标底下 pointermove 就不触发导致停住）
- **房顶放置对齐**：PlacePlane 平面高度按 placement 计算——ceiling 用房顶高度平面、wall 用墙面高度平面，俯视时鼠标投射到正确高度、模型和鼠标对齐（原来房顶模型放地面平面，透视下错位）
- **家具面板 ±按钮**：3D 家具面板高度/旋转/缩放加 − ＋ 按钮（高度±0.1m、旋转±15°、缩放±0.1）
- **⚠️ 教训**：①拾起跟随便用 useFrame+state.pointer 全局跟踪，别靠 onPointerMove（元素离开鼠标就断）；②放置平面高度要跟 placement 走，俯视透视下地面平面会导致房顶模型错位；③右键取消要统一 tool='select'+清三态(selected/wallSel/pickItem)
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 23:05 · 右键区分单击/长按 + 设备易选中 + 家具地面/屋顶

- **右键区分单击/长按**：右键取消改成 pointerdown/up 记录位置——移动<5px 才是「单击」才取消；按住右键拖动（平移）移动>5px 不算取消。之前 contextmenu 一按右键就取消，长按平移也误触取消
- **设备容易选中**：DeviceMarker 加透明点击球（半径0.32，opacity0），吊扇这种薄片/小球设备也点得到
- **家具地面/屋顶按钮**：3D 家具详情面板加「位置：地面/屋顶」按钮——地面=pos[1]=0+placement floor，屋顶=pos[1]=层高+placement ceiling
- **⚠️ 教训**：①右键取消要区分「单击」和「拖动」——用 pointerdown/up 记录位置差，移动>阈值算拖动不算取消；②小设备（吊扇薄片）加透明点击球扩大 raycast 目标；③地面/屋顶=改 pos[1]+placement 两个字段
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 23:10 · 设备对齐普通模型（可拖可调）

- **设备对齐普通模型**：设备(DeviceMarker)原来只单选不能拖、面板只有高度/删除。现在和家具一样——加 rot/scale（默认0/[1,1,1]）应用到 group；移动工具单击拾起/再单击放下、双击切移动拾起、按住拖；useFrame 跟随。详情面板补齐：高度±按钮、位置(地面/屋顶)、旋转±、缩放±
- **⚠️ 教训**：设备和家具在 3D 里应该是同一套交互（选/拖/调属性），别给设备做「特殊版」——用户明确要求「按普通模型一样，不要整特殊」
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 23:24 · 墙全色系选色器 + 常用色收藏 + 壁纸贴图

- **全色系选色器**：墙面板颜色从几个默认色扩展成蜂窝网格——FULL_PALETTE 生成（每个色相 15° 三档明度 + 灰度，~80 色），grid 网格 16px 方块点选
- **常用色收藏**：选中色后点「☆ 收藏/★ 已收藏」toggle，常用色存 localStorage（`ai3d_fav_colors`），单独一行显示
- **壁纸贴图**：墙加 `texture` 字段（图片名，复用 /api/background 上传），面板「上传壁纸/更换/清除」。3D 渲染 `WallMaterial` 组件用 TextureLoader 加载图片做 map（有贴图时颜色置白，贴图原色显示）
- **⚠️ 教训**：①选色器用 grid 网格方块，别用一堆圆形 swatch；②常用色存 localStorage（客户端偏好）；③贴图用 map + color 置白，否则贴图被墙色染色
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 23:35 · 墙色蜂窝布局 + RGB 输入 + 壁纸贴图修复

- **蜂窝布局**：全色系改成正六边形色块（clip-path polygon）错排成蜂窝状（奇数行左移 10px + 负 margin 重叠）
- **RGB 输入**：颜色加 RGB 三个数字输入（THREE.Color 解析当前色转 RGB，改后转回 hex）
- **壁纸贴图修复**：之前壁纸不显示根因是 `meshPhysicalMaterial` 始终 `transparent opacity depthWrite=false`——全透明时贴图被透明排序/深度问题搞没了。改成 `meshStandardMaterial`，`transparent` 只在 opacity<1 时才开，opaque 时 depthWrite=true，贴图 RepeatWrapping 平铺
- **⚠️ 教训**：贴图不显示先查「material 是否被 transparent+depthWrite=false 搞坏」，全不透明时别开 transparent
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 23:42 · 3D 墙选中高亮 + 批量选择批量改属性

- **墙选中高亮**：3D 选中的墙 emissive 发蓝光（WallMaterial 加 selected 参数，选中 emissive #2f7fe0）
- **3D 墙多选**：墙点击改成和 2D 一致——Ctrl/Cmd 或「多选」开关 toggle 到 wallSel，普通点击单选（也存 wallSel[0]）。WallProps3D 改成读 wallSel：length=1 显示单墙面板，length>1 显示「已选 N 面墙」批量面板（高度/厚度/颜色/全色系/不透明度，setAll 对所有墙 Object.assign）
- **⚠️ 教训**：3D 和 2D 墙选择统一走 wallSel，别 3D 用 selected、2D 用 wallSel 两套；批量改属性 = filter 出墙数组 + 逐个 Object.assign
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-14 23:52 · 墙面板修复 + 选中线框高亮 + 大六边形色盘 + 壁纸缓存

- **墙面板不显示修复（根因）**：Viewer 渲染 WallProps3D 的条件是 `selected && ['wall','furniture','device'].includes(selected.type)`，但墙改走后 wallSel（selected=null）→ 条件永远 false → 面板不显示。改成 `editing && <WallProps3D/>`，由 WallProps3D 自己判断返回 null
- **选中线框高亮**：选中的墙外圈加蓝色 wireframe 线框（scale 1.08），透明墙也看得清（之前只靠 emissive 太淡）
- **大六边形色盘**：全色系改成上下短中间长的六边形排布（lens=[7,9,11,13,13,11,9,7]），小六边形 clip-path 色块错排成一个大六边形
- **RGB 加宽**：RGB 三个数字框 42→52，能显示三位数
- **壁纸缓存**：贴图 URL 加 `?t=Date.now()` 防缓存 + material key 用 map.uuid 强制重建
- **⚠️ 教训**：①面板渲染条件改选择模型（selected→wallSel）后，父组件渲染条件也要跟着改，别留旧条件；②选中高亮别只靠 emissive，加 wireframe 线框才明显；③色盘六边形用 lens 数组（上下短中间长）排
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 06:19 · 视角跨设备 + 3D背景缩略图/默认按钮 + 墙壁纸缩略图

- **视角跨设备**：customViews 从 localStorage 改存 settings（`settings.customViews`，App 启动加载，BottomBar 保存/删除时 api.saveSettings），换设备也能看到
- **3D 背景默认按钮 + 缩略图**：3D 编辑「背景」弹窗加「默认」按钮（恢复蓝色）+ 已上传背景缩略图列表（点选/✕删，复用 /api/backgrounds + /api/background/delete）
- **墙壁纸缩略图**：墙面板「壁纸」加已上传图片缩略图（点选直接当墙贴图），上传后也刷新缩略图列表
- **⚠️ 教训**：跨设备的配置（视角/常用色等）存 settings（后端），别存 localStorage；缩略图列表用 /api/backgrounds 复用，上传后 setBgList 头插新图
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 06:39 · 3D屋顶开关 + 2D取景含墙

- **3D 屋顶开关**：store 加 `showCeiling`，Room 渲染房间顶面（floorGeo 放层高高度、半透明、DoubleSide、可开关），BottomBar 加「🏠 屋顶」按钮
- **2D 取景含墙**：floorBounds 加 walls 参数，取景时把墙也纳入（排除草稿墙 draft.walls，避免画墙时视口缩到第一段墙）。这样只画墙没房间时户型也在画布中央
- **六边形 HSV 色盘**：单独写了一个原生 JS 单文件 HTML 到 `~/Desktop/hexagon-color-picker.html`（无外部库，六边形色盘+明度滑块+HSV/hex显示+hex输入+拖拽拾取，N=6 改 8 就是八边形）
- **⚠️ 教训**：取景要把「墙」也算进去（不只房间/家具/设备），否则只画墙不闭合时户型跑出画布中央
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 06:51 · 六边形色盘进设置调背景色 + 屋顶语义分开

- **六边形色盘进设置**：新建 `HexColorPicker` React 组件（canvas 逐像素画六边形，角度=色相/距离=饱和度/明度滑块/hex输入，复用桌面 HTML 逻辑），加到主界面「设置→背景效果→纯色」下，选的颜色存 `settings.bgColor`。SceneBackground 纯色模式用 bgColor 渲染
- **屋顶语义分开**：主界面（非编辑）屋顶=封闭房间顶上加半透明顶面（Room 里 `showCeiling && !editing`）；3D 编辑屋顶=层高高度加一个和下面地板对应的网格画布（gridHelper，方便摆放吊顶/吊灯）。两者都是同一个「屋顶」按钮，按 editing 状态切换
- **⚠️ 教训**：①同名「屋顶」在不同场景语义不同——主界面是房间顶面、编辑是摆放参考网格，别混成一个实现；②色盘移植到 React 用 useRef+canvas 逐像素绘制，N=6 改 8 就是八边形
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 07:15 · 背景简化成色盘+上传图

- **背景简化**：去掉纯色/背景图/渐变/夜景四选一，改成「色盘 + 上传图片」两样——有图用图、没图用色盘颜色。SceneBackground 简化为 `bgImage ? 图片 : bgColor`；设置面板去掉 bgMode 按钮，色盘和上传图片始终显示。色盘加黑白灰快捷色块
- **⚠️ 教训**：用户明确「色盘能表达所有颜色就别要夜间/渐变那些」，按用户意图删掉多余模式，别留着
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 07:25 · 修Room hooks顺序(黑屏)+渐变背景+色盘覆盖修复

- **修 Room hooks 顺序（黑屏根因之一）**：Room 组件原来 `useMemo` 在 `if(pts.length<3) return null` 早退之后，房间点数变化时 hooks 数量变化会崩。改成 useMemo 提前（pts<3 时返回 null geometry），早退放后面
- **渐变背景**：加 bgGradient1/bgGradient2，设置面板加「渐变」板块（两个色盘选两色），SceneBackground 加 gradient 分支（两色垂直渐变 CanvasTexture）
- **色盘覆盖修复**：点色盘时清空 bgImage 并 bgMode='color'（之前有背景图时点色盘没反应，因为 bgImage 优先）
- **⚠️ 教训**：hooks 不能放在条件早退之后，否则组件点数变化时 hooks 顺序变会崩（黑屏/报错）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 08:21 · 泛光 Bloom 后期处理 + 模式简化

- **泛光 Bloom**：Viewer 加 `Effects` 组件（three 自带 EffectComposer + RenderPass + UnrealBloomPass + OutputPass），`useFrame(cb, 1)` 接管渲染循环。发光设备/灯带/空调气流有柔和光晕；色调映射设 ACESFilmic（曝光1.1）更电影感。BottomBar 画质菜单加「✨ 泛光」开关，关时 bloom pass strength=0
- **模式简化**：底部栏模式从 6 个（全屋/照明/遮阳/环境/安防/纯户型）简化成 2 个（全屋/结构）。照明等 5 个只是灯光色调滤镜，内容不变，删了；纯户型改名「结构」（隐藏家具设备只看结构）
- **⚠️ 教训**：①R3F 后期处理用 `useFrame(cb, renderPriority=1)` 接管渲染（R3F 不再自动 render），否则双重渲染泛光不生效；②泛光开关直接改 UnrealBloomPass.strength=0 最省事，不用重建 composer
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 08:50 · 设置三 tab + 3D 导出 + 地板厚度/色盘/壁纸 + 家具锁定

- **设置三 tab**：设置弹窗从「一坨」拆成 3 个 tab（默认选项 / 背景配置 / 分享），`settingsTab` state 切换。默认选项=原来那套默认值；背景配置=背景色盘/渐变/屋顶等；分享=导出
- **3D 导出（分享 tab）**：`export3DPng(count)` 用 `.canvas-wrap canvas` 的 `toDataURL` 下载 PNG（count>1 时开 autoRotate 每 900ms 截一张不同角度）；`record3DVideo(seconds)` 用 `canvas.captureStream(30)` + MediaRecorder（优先 mp4，退回 webm）录旋转视频
- **地板（房间）属性升级**：房间面板加「厚度」输入框（thickness）；颜色从 8 色 swatch 换成 HexColorPicker 六边形色盘；新增「壁纸」上传（POST /api/background）→ 设 `room.texture`，下方展示已上传背景缩略图（复用 /api/backgrounds，点选直接当地板贴图）
- **家具锁定**：3D 里 move 工具点已锁定的家具时 `item.locked` → toast「已锁定，无法移动」并 return，不拾起
- **⚠️ 教训（血泪）**：Scene.jsx 加 `toast('已锁定…')` 但**没在顶部 import toast**（原 `import { useStore, setState, getState }`），build 能过但运行时点锁定家具会 `toast is not a function`。**新代码用到的每个函数确认已在 import 里**（跟 1.1.18 的 `re` 缺失同类）
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 09:05 · 修复 3D 家具选定不了

- **现象**：用户报「3D 模式下家具选定不了」
- **根因（两个）**：
  1. **墙多选残留盖住家具面板**：选墙时设 `wallSel`，再点家具设 `selected` 但没清 `wallSel`。`WallProps3D` 里判断顺序是 `wallSelIds.length===1`（墙面板）在 `selected.type==='furniture'`（家具面板）**之前**，所以墙面板一直显示，家具面板出不来 → 看起来「选不中家具」
  2. **2D→3D 切视图没重置工具**：Editor 的「3D」按钮只 `setState({ view2d:false })`，工具还是刚才的「家具/墙体」等放置工具 → 3D 里点家具的 onClick 只处理 `move/select/delete`，其他工具直接没反应
- **修复**：①`handleSelect` 选中家具/设备/房间时 `setState({ selected, wallSel: [] })` 清空墙多选；②「3D」按钮 `setState({ view2d:false, tool:'select', pickItem:null, wallSel:[] })` 重置为选择工具
- **⚠️ 教训**：①多类对象（墙/家具/设备/房间）共用 `selected`+`wallSel` 两套状态，切对象时**要清掉另一套**，且注意 WallProps3D 里墙面板判断排在对象面板之前；②切视图/上下文时重置工具为「选择」，否则残留的放置工具会让「点选」静默失效
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 09:40 · 模型锁定 + 地板壁纸 + 浏览工具 + 底部按钮合并进设置

- **模型锁定**：家具详情面板（2D PlanEditor + 3D WallProps3D）加「🔒 锁定/🔓 解锁」开关，存 `f.locked`；Editor 顶部工具条加「全部锁定/全部解锁」（锁当前楼层所有家具）。锁定后：2D `startDrag` 和 3D move 工具 onClick / onPointerDown 都 toast「已锁定」并 return
- **地板壁纸（补完）**：之前只存 `room.texture` 不渲染。现在 3D 加 `FloorMaterial`（复用 WallMaterial 的 TextureLoader 逻辑，有贴图用贴图/没贴图用纯色）；2D 加 SVG `<pattern>`（`room-tex-{id}`，1m 平铺）+ 房间多边形按 `room.texture`/`room.color` 内联 fill；WallProps3D 加「房间」面板（名字/面积/颜色 HexColorPicker/壁纸上传+缩略图）
- **浏览工具**：左侧工具栏最上方加「浏览」（tool='browse'，H 键），只操作视图不选中。3D 走默认 orbit（左键旋转右键平移）；2D 把 browse 加进 `handleFloorDown` 的平移条件；`setTool('browse')` 清 selected/wallSel/pickItem
- **底部按钮合并进设置**：删底部「分享」「画质」按钮 + 对应函数；设置弹窗从 3 tab 扩到 4 tab（默认/背景/**画质**/分享），画质 tab=画质档位+投影+泛光（实时）；设置按钮加齿轮线画 SVG 图标；旋转按钮加线画 SVG 图标 + 拆出「停止」按钮（旋转只负责顺逆时针切换）
- **主界面禁止选墙**：Scene 墙 onClick 加 `!editing ? undefined : ...`，非编辑模式点墙不选中（编辑模式才能选墙改属性）
- **⚠️ 教训**：①地板壁纸要「存纹理」+「渲染纹理」两步都做，只存不渲染=白做；②同名「画质」在默认选项（下次生效）和画质 tab（实时）语义不同，别混；③删底部按钮时连它的 state/函数/import 一起清，否则留死代码
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 10:10 · P0 设备控制（对齐 3D未来家）

- **灯色温/颜色/亮度联动**：`DeviceModel` 加 `state` 参数，灯颜色优先 `rgb_color`（rgbToHex）→ `color_temp_kelvin`（kelvinToHex 色温近似）→ 默认黄；emissive 用 `{light}` 反映真实颜色。控制面板加亮度滑条（0-255）/色温滑条（2000-6500K）/颜色色块+原生取色器，走 `light.turn_on` 带 data
- **窗帘开合动画**：cover 用 `current_position`(0=关,100=开) 或 state 算 `openRatio`，百叶帘叶片按 `(1-openRatio)` 铺开/收起、单向窗帘面板按比例铺满/收到一侧。控制面板加开/停/关按钮
- **门开合动画**：binary_sensor.door 改成「门框 + 门扇」，state on=开 时门扇绕铰链转 1.9 rad
- **点击实体弹控制面板**：DeviceModal 从「只有切换」扩成完整 overlay：开关 + 灯(亮度/色温/颜色) + 窗帘(开停关)
- **场景同步**：`haEntities` 过滤 `scene.*`，BottomBar 加「🎬 场景」按钮 → 场景列表 → `scene.turn_on` 一键激活
- **通知中心**：server.py 加 `/api/ha/persistent_notifications`(GET)+`/api/ha/persistent_notification/dismiss`(POST)；BottomBar 加「🔔 通知」按钮 → 通知列表 → 可 ✕ 关闭
- **后端 service 扩展**：`/api/ha/service` 加 `data` 透传（`payload.update(data)`），支持 light.turn_on 带 rgb_color/brightness/color_temp_kelvin
- **⚠️ 教训**：①service 调用要支持「带参数的调用」（不只 entity_id），灯调色/调亮度全靠它；②cover 动画用 `current_position` 属性最顺滑（HA 开合过程中它自己渐变），别只看 state；③多面板（场景/通知）开关放 store（跟 settingsOpen 一样），BottomBar 用 setState 触发
- **验证**：build 通过 ✓；server.py py_compile ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 10:40 · P1/P2：缩放区域 + 太阳日照 + 房间模板 + 家具分组

- **缩放区域（P1）**：查看态点房间 → `handleSelect` 算房间中心/包围盒，设 `camTarget`+`camDist`+触发 `camViewSignal:{type:'default'}`，相机飞到房间（iso 视角）。编辑态点房间仍只选中不跳视角
- **太阳/天空/日照（P1）**：`sun.sun` 从 `haEntities` 取，`sunAuto`(默认 true) 自动 `night = state!=='above_horizon'`，手动切日夜时设 `sunAuto:false`；`sunElevation` 存高度角。Scene 加 `SkyDome`（大 BackSide 球 + CanvasTexture 垂直渐变：日夜/日出日落暖色）+ 平行光位置/颜色按高度角调
- **房间模板（P2）**：Editor 顶部「📐 房间模板」下拉，7 个标准尺寸矩形（卧室/次卧/客厅/厨房/卫生间/书房/餐厅），`insertRoomTemplate` 在户型右侧放 4 面墙 → `recomputeRooms` 自动识别成房间
- **家具分组（P2）**：家具加 `group` 字段（2D/3D 面板都有「分组」输入）。2D `startDrag`/`handleMove` 存 `{members,offsets}` 按相对偏移一起动；3D `Furniture.moveItem` 算 delta 应用到同 group 家具
- **⚠️ 教训**：①相机跳转要「设 camTarget/camDist + 触发 camViewSignal」，只设 camTarget 相机不动；②天空球 BackSide + `fog:false` + `depthWrite:false`，否则被雾/遮挡吞掉；③分组移动要存「相对偏移」（每个成员相对拖拽物的 delta），不能简单统一坐标
- **验证**：build 通过 ✓；部署 v0.1.0-p40 健康 ✓

## 2026-08-15 11:20 · 状态实时化（WebSocket 替换轮询）

- **目标**：原来后端每 3s 拉 HA REST、前端每 5s 拉后端，开关灯最多延迟 5s。改成 HA WebSocket 实时推送
- **后端**：`ws_loop()` 后台线程用 `websockets` 库连 HA `ws://host:8123/api/websocket`，`auth` + `subscribe_events(state_changed)`，收到事件 `CACHE.update(eid, new_state)`（只更新绑定的实体）并 `notify_stream()` 推给前端。`CACHE.update()` 只认 `_bound` 里的实体
- **SSE 推送**：新增 `/api/ha/stream`（`_send_stream`），`Content-Type: text/event-stream` + `X-Accel-Buffering: no`，把 `snapshot`/`state` 事件推给所有 EventSource 客户端。初始快照 + 每次 state_changed 推单实体
- **前端**：`EventSource(BASE+'api/ha/stream')`，收到 snapshot 整替换 `haStates`，收到 state 只改单个实体。**兜底**：EventSource 断了自动重连 + 原 5s 轮询仍在
- **Dockerfile**：`apk add py3-websockets`（后端零第三方依赖被打破，改成「websockets 可选，不可用退化为轮询」）
- **⚠️ 教训**：①加新依赖后第一次部署会因 Docker 拉包超时（构建 >600s），重跑一次（构建缓存）就好——第一次部署慢属正常，别以为是坏了；②SSE 过 HA ingress 要 `X-Accel-Buffering: no` 防缓冲；③WebSocket 订阅要「优雅降级」：lib 缺失→退轮询、连不上→重试、前端断→EventSource 自动重连
- **验证**：健康 `{"ok":true,"ha":true}` ✓；curl `/api/ha/stream` 返回真实状态快照 ✓；部署健康 True ✓

## 2026-08-15 11:40 · 代码分割（拆 three/react chunk）

- **做法**：vite `rollupOptions.output.manualChunks` 按 `node_modules/three` → `three`、`node_modules/react`+`@react-three` → `react-r3f` 拆成独立 chunk
- **结果**：1 个 1.24MB 拆成 3 个——app 代码 197KB（每次更新都变）+ react-r3f 272KB + three 770KB（后者很少变）
- **收益**：app 更新时浏览器只重下 197KB，不用再下 1.24MB（three/react 被缓存）；并行下载。**不减少首屏总大小**（总量不变，只是分包）
- **⚠️ 教训**：manualChunks 只优化「缓存 + 并行」，不减少首屏体积；要真正减首屏得靠 Draco 压缩（用户暂缓）或按需懒加载
- **验证**：build 通过 ✓；index.html 相对路径引用 3 chunk ✓；部署健康 True ✓

## 2026-08-15 12:55 · 代码分割导致黑屏 → 回滚

- **现象**：代码分割部署后，打开面板全黑屏（React 没渲染出来）
- **根因**：`manualChunks` 把 three/react 拆散后 chunk 加载出错（模块循环依赖/加载顺序），浏览器报错导致 React 不挂载。试了「vendor 全部第三方」的改法，vendor chunk 哈希不变（内容一样），没解决
- **修复**：**回滚代码分割**，恢复单 chunk（`index-BZcD_Kmg.js` 1.24MB），黑屏消失
- **⚠️ 教训（血泪）**：①代码分割/分包这类改动，**不能只 build 通过就上**——build 过了不代表 chunk 能正确加载，必须实际浏览器打开验证（这次就是 build 过了、部署健康 True，但黑屏）；②react 生态拆 chunk 要特别小心循环依赖（react-dom→scheduler），拆散就挂；③以后做分包先本地 `vite preview` 用浏览器点一遍再上
- **验证**：回滚后部署健康 True ✓

## 2026-08-15 13:30 · 黑屏真凶：sunEnt TDZ 错误（不是代码分割）

- **真根因**：上一轮误判为代码分割。实际是 P1 太阳日照功能——`const sunEnt = (haEntities||[]).find(...)` 声明在**组件体第 409 行**，但「自动切日夜」的 `useEffect` 在**第 326 行**，它的依赖数组 `[sunEnt && sunEnt.state, ...]` 执行时 `sunEnt` 还没声明 → `ReferenceError: Cannot access 'sunEnt' before initialization`（TDZ）→ React 崩 → 黑屏
- **修复**：把 `const sunEnt` 移到 useEffect **之前**
- **⚠️ 教训（血泪）**：①`useEffect` 的**依赖数组是立即求值的**，它引用的 `const` 必须在 useEffect 之前声明（不是只有 callback 里用到才要紧）；②诊断黑屏别猜——用无头浏览器打开，`#root` 内容长度 0 = React 崩，看 console 报错就知道真因；③「build 通过 + 健康 True」≠ 页面能渲染，交付前必须真打开验证
- **验证**：无头浏览器实测——修复前 `#root` 长度 0、1 条致命错；修复后 3711、0 错误 ✓；部署健康 True ✓

## 2026-08-15 14:00 · 通知实时推送 + 场景/通知改右侧透明面板 + 底部图标全换线图

- **通知实时推送**：后端 `ws_loop` 加订阅 `call_service` 事件，过滤 `notify.*` / `persistent_notification.create`，推 `notification` SSE 事件；前端 EventSource 收到后 `toast` 弹提示。只显示「当下发生的」，历史仍只有持久通知能查
- **场景/通知改右侧半透明面板**：从居中 modal 改成 `position:fixed; right:14px` 的 `.side-panel`（半透明 `rgba(14,22,40,.5)` + backdrop-blur），不再挡全屏。场景/通知按钮改成 **toggle**（`setState(s=>({sceneOpen:!s.sceneOpen}))`，再点一次关闭）
- **底部工具栏图标全换线图 SVG**：抽了个 `Ic` 组件（stroke=currentColor 统一风格），把 🎬🔔🧱🚪🏠👁⛶⌖☀️🌙💾📷 等 emoji 全换成 line icon（layers/bell/brick/door/home/eye-off/expand/crosshair/sun-moon/floppy/camera 等）
- **验证**：无头浏览器 `#root` 长度 7193、0 错误 ✓；部署健康 True ✓

## 待办 / 阶段规划
- UI 打磨：对齐原版视觉（图标、配色、间距、材质）
- 功能对齐：设备实时状态细化、Area 绑定建议、最近备份

## 决策记录
- 2026-08-12 18:10 用户拍板：自建「AI 3D Home」，功能+UI 布局与 JMGLink 一致，先复刻后优化，逐步推进
- 2026-08-12 19:30 技术栈确认：React + R3F；推进方式：连续推进（本地做完再部署）

## 2026-08-15 深夜 · 本地部署通道 + 房间壁纸/尺寸 + 检测修复 + 一堆UI

- **本地部署通道**：`deploy_local.py`（build→打包→上传/api/deploy→重启，15秒），不走 GitHub。run.sh 优先读 /share 里的 server.py/webui；server.py 加 /api/deploy 接口；模型/固定从镜像读（/models/）
- **房间壁纸修复**：robustFloorGeometry 加 UV（之前没UV导致3D贴图不显示）；recomputeRooms 保留 texture/thickness/opacity
- **房间长宽高厚度透明度可编辑**：resizeRoom（包围盒缩放墙）+ NumInput 组件（本地缓冲）
- **检测/修复**：零长度/重复/重叠删、伸出墙修剪（排除连接端交点）；问题墙标色+标签；墙体交点红点
- **场景面板对标JMGLink**：图标+执行中/刚刚执行+圆形执行按钮；场景弹窗只在主界面
- **设置默认选项**：每条一行 + 泛光/帧率卡顿/显示墙门窗屋顶工具栏场景等开关；HA连接回左上角（闪烁绿点）
- **底部工具栏**：可排序/显示隐藏（「工具栏」按钮）
- **⚠️ 未解决**：①detectRooms 漏检「中间上方」房间（共线墙+T形连接，见技术笔记）②左键拖视角偶尔跳回（根因未定位）

## 2026-08-15 20:21 · 房间识别换成 half-edge DCEL（修复漏房间）

- **问题**：`detectRooms` 对「共线墙 + T形连接」漏检一个房间——真实户型 13 面墙只识别 4 个，缺「中间上方」X[-7,-2] Z[-5.97,-0.1]
- **根因**：旧版用「平面图邻接 + 精确坐标」找封闭环，用户拖墙端点差 2-4mm（-8.98/-5.97/-5.1 都是非整值），本应重合的点被算成相隔几毫米的两个点 → 图拓扑断裂 → 漏房间
- **修复**：换成 half-edge DCEL（容差吸附）——①T形吸附：端点几乎碰到别的墙就投影到那条墙；②union-find 按 2cm 容差聚类顶点（簇质心）；③墙段在落在其上的顶点处切分成原子半边；④右手规则面遍历提取封闭环。`detectRooms` 签名不变，`pointToSeg`/`segmentIntersect`/`pointOnSeg` 等别处依赖的导出没动
- **顺带修 `cleanLoop`**：共线判断从绝对叉积 `≤1e-5` 改成「点到直线距离 ≤1cm」，清掉吸附残留的 1mm 共线点（否则房间会多出 1 个顶点）
- **⚠️ 教训**：①拖墙产生的非整坐标是「拓扑断裂」元凶，房间检测必须先做容差吸附，不能靠精确坐标；②build 通过 ≠ 算法对，要拿真实户型 JSON 单独 `import geometry.js` 跑 `detectRooms` 验证
- **验证**：真实数据 13 面墙 → 5 房间全出（含缺失的中间上方）；回归测试（单矩形/田字格/L形/带2-4mm误差矩形/两房间共享墙）全通过；build ✓；无头浏览器 `#root` 7790、0 错误 ✓；部署健康 ✓

## 2026-08-15 20:46 · 红点容差统一 + 红点开关按钮 + 房间缩放推开邻居

- **红点容差统一**：`SNAP_TOL` 改为导出，红点（PlanEditor.jsx）去重从 1cm（`round(p*100)`）改成 `round(p/SNAP_TOL)`、T形连接从 `pointOnSeg`（精确）改成 `pointToSeg(...).dist < SNAP_TOL` 投影。之前红点(1cm)和检测(0.1mm)两套容差打架，导致「红点亮了房间却不出」，现在统一 2cm
- **红点显示/隐藏按钮**：store 加 `showIntersections: true`，工具栏「交点」按钮切换（和「尺寸」并列），红点渲染 `{showIntersections && ...}` 控制显隐
- **房间缩放推开邻居**：`resizeRoom` 之前只缩放本房间边界墙，共享墙一动相邻房间就被压扁。改成：本房间边界墙中心缩放（放大向外），相邻房间的墙按「整段平移」（`allLeft/allRight/allAbove/allBelow` 判断墙整体在边界外侧）推开/拉近，保持相邻房间大小不变。放大中间房间 → 左右房间整体让位；缩小 → 拉回
- **⚠️ 教训**：①「缩放」和「平移」是两个操作，缩放房间时要分清「动自己」还是「动邻居」；②对正交矩形户型用「墙整体在某边界外侧」的坐标判断即可级联推开，不用建房间邻接图；斜墙户型这个判断会漏（先接受）
- **验证**：三房间一字排开，放大中间长 3→4 → 左[-0.5,2.5] 中[2.5,6.5] 右[6.5,9.5]，左右长保持 3；缩小 4→2 左右拉回。build ✓；无头浏览器 `#root` 7790、0 错误 ✓；部署健康 ✓

## 2026-08-15 21:22 · 房间缩放重写为「分段坐标变换」（BFS/切分方案翻车）

- **背景**：上一版 resizeRoom 用「坐标判断平移邻居」，用户实测放大缩小来回整，墙分家了（改宽时左右邻居的共线上下墙被误判成上下邻居，错位 0.5m）
- **翻车过程**：先试「切分贯穿墙 + BFS 传播」，两个坑：①共线误差——房2 下边界墙 z 有 0.005 误差，`segmentIntersect` 把它当「穿过」下边框，误切出假交点（x=-5.203）导致墙段错位；②纯平移判断容差 1e-6 太严——右边界墙 x 有 0.001 误差，缩放后两端位移差 0.0012，被误判成「拉伸」、没推右邻居；③共享墙（房2 下边界=中下上边界）拉伸后中下左边界不动 → 中下左上角断开 → 5 房间并成 3 房间
- **最终正解（分段坐标变换）**：`resizeRoom` 不再切分、不再 BFS，直接对每个墙端点做分段变换——`x<=minX+eps` 左移 dx、`x>=maxX-eps` 右移 dx、`z<=minZ+eps` 上移 dz、`z>=maxZ-eps` 下移 dz。这样所有共享角的墙端点做同样的变换，缩放后永远重合 → 户型永远封闭
- **⚠️ 教训（血泪）**：①房间缩放的**唯一正确思路是「对墙端点做分段坐标变换」**，不是「移动墙/切分墙/推邻居」——端点变换天然保证共享角重合，切分+BFS 是过度设计还一堆坑；②贯穿墙不用切分：分段变换对贯穿墙两端点都做同样变换（都在 x=minX 侧），墙整体平移，detectRooms 自动切分；③「保证封闭」的核心是「共享顶点的端点一起动」，分段变换天然满足
- **验证**：真实户型（13 面墙含贯穿墙）放大长 5→6 保持 5 房间封闭、左右邻居整体让位大小不变、缩小 6→5 回到原位；三房间改宽 2→3 左右邻居横向不变。build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-15 21:32 · 项目弹窗加「保存项目」+ 修画墙闭合后视角突变

- **保存项目按钮**：项目弹窗（Editor.jsx）「新建项目」旁加「保存项目」按钮，点击 `saveNow()`（存后端）+ `createBackup()`（生成带时间戳快照）+ `loadBackups()`（刷新最近项目列表）。之前工具栏已有「保存」，这次补进项目弹窗
- **画墙闭合后视角突变 bug**：根因是 PlanEditor 的 `bounds`（户型取景范围）用 `draft?.walls` 排除草稿墙——画墙过程中视口稳定，但**画完闭合瞬间 `setDraft(null)`**，草稿墙从「排除」变「纳入」，`bounds` 突变 → viewBox 跳 → 视角切换
- **修复**：引入 `hiddenWalls` state 单独存「本次画墙的墙 id」，`bounds` 改用它排除。画墙加墙时累积 id、画完闭合**不清空**（取景不突变），直到「居中」按钮或切换出画墙工具（`tool !== 'wall'`）才清空纳入取景
- **⚠️ 教训**：`useMemo` 计算取景范围时，别用「临时状态（draft）的有无」决定排除与否——状态一清空就突变。要排除的墙集合独立存一个 state，由显式动作（居中/换工具）控制何时纳入
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-15 22:00 · 画布跳转彻底修（冻结快照）+ 房间尺寸锁定

- **画布跳转彻底修**：之前 hiddenWalls 只排除了墙、没排除房间，画完闭合 `recomputeRooms` 生成的新房间 points 还是撑大取景。改成 `frozenRooms/frozenWalls` 两个 state，画第一面墙时**冻结 rooms+wals 快照**，`bounds` 用冻结快照计算（画墙期间含画完闭合取景完全不变），「居中」或切换楼层才解冻。这才是正解——之前只冻墙不冻房间是半吊子。**后续修正**：最早还留了「切出画墙工具就解冻」的 useEffect，导致画完房间一切工具又跳——用户要求「不点居中就不自动居中」，删掉切工具解冻，只保留「居中」和「切楼层」解冻
- **房间尺寸锁定**：房间对象加 `locked` 字段（recomputeRooms 保留），房间属性面板加「尺寸锁定」开关。resizeRoom 逻辑：①先切分贯穿房2 边框的墙（锁定房间边界段才能独立识别，整条边界墙跳过防共线误切）②锁定房间**同方向**整体平移保持尺寸（改长推左右、改宽推上下）③跨方向（改长影响上下、改宽影响左右）走分段变换共线联动，保持户型封闭
- **⚠️ 教训（几何根本）**：①「锁定房间保持尺寸」只在**同方向**（缩放方向和邻居方向一致）可行；**跨方向**（改长时上下邻居共享横墙、改宽时左右邻居共享共线边界）锁定尺寸 + 保持封闭 = 几何矛盾（共享墙既得拉伸又得不动，除非拆墙），只能二选一——选了「跨方向走共线联动（保持封闭、锁定暂时失效）」；②贯穿墙必须先切分才能识别「哪段属于哪个房间」，这是「墙=线段」数据模型的固有代价
- **验证**：锁定房1左上（左右邻居）改房2长→整体左移、长保持5、5房间封闭；锁定中下（上下邻居）改房2长→跨方向走共线联动、5房间封闭不分家。build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-15 22:20 · 锁定房间「自己的长宽」也不能改

- **补充**：之前锁定只做了「相邻房间缩放时保持尺寸」，漏了「锁定房间自己不能改长宽」。用户反馈锁定后房间详情里长宽还能改
- **修复**：①`resizeRoom` 开头加 `if (room.locked) { toast('该房间已锁定尺寸，先解锁再改长宽'); return }`（双保险，任何调用都拦截）；②房间详情「长/宽」的 −/＋ 按钮和 NumInput 加 `disabled={selRoom.locked}` + 变灰（NumInput 组件补了 disabled 参数）
- **教训**：「锁定」要同时覆盖两层——①被锁房间自己不能改；②相邻房间缩放时被锁房间保持尺寸。之前只做第二层，用户点锁定后还能改自己，等于没锁

## 2026-08-15 23:00 · 跨方向锁定的完整实现（通用补段）

- **目标**：锁定房间尺寸绝对不变（跨方向也保持），且户型封闭。之前「跨方向走共线联动」会让锁定房间跟着变，用户不接受
- **实现**：①锁定墙完全不动（不参与分段变换）；②同方向整体平移；③**通用补段**——锁定墙端点不动，缩放前与它重合的未锁定墙端点移动后错位，在错位处补新墙段。这样锁定房间所有边界（含和别的房间共享的墙）的错位都能补上
- **⚠️ 关键坑**：补段必须用**缩放前**记录的未锁定墙端点（`preEndpoints`）判断「原本重合」。之前直接在分段变换后遍历墙端点，端点已移动、`dist<eps` 永远 false、补段数=0、白补
- **结果**：锁定中下（上下邻居）改房2长 5→6：中下长保持5、房2长6、5房间封闭、补4段。但「和锁定房间共享墙的第三房间」（房1左下）会连坐变形（长5→5.5）——这是共享墙的固有限制
- **验证**：真实户型锁定中下改房2长 → 补段4、5房间封闭、锁定房间长5不变。build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-15 23:20 · 共线约束修正 + 墙的移动/旋转交互

- **共线约束**：原来 `collinear` 是「第2条线接到第1条线终点、沿方向延长」（首尾相接）。用户纠正：共线 = 两条线在同一条直线上、方向对齐，**不要求线段重合**。改成「b 方向对齐 a 方向（±da），b 起点投影到 a 直线上，保持 b 原朝向和长度」
- **墙交互**：①双击墙 = 选中 + 切移动工具（`doubleClickDrag` 加 wall 分支，设 `wallSel:[id]` 而非 selected）；②移动工具下点起点/终点小圆点拖动 = **旋转**（绕另一端、保持长度，`startWallDrag` 从「直接改端点坐标」改成「归一化方向×原长」）；③移动工具下点墙本身拖动 = **移动**（新增 `wallMoveRef`，用「初始墙位置+鼠标位移」整体平移避免累积误差）
- **坑**：双击墙设 `selected` 会导致 `selW`（依赖 `wallSel`）仍为 false、起点终点手柄不显示，必须设 `wallSel:[id]`
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-15 22:35 · 画墙视口跳转的真正根因（冻结快照被反复赋值）

- **现象**：连续画多个房间，画完一个接着画下一个时，视口突然跳（用户反复报「画完封闭图形就跳转」）
- **真根因**：冻结快照的逻辑写成了「每画一个新房间的第一面墙就 `setFrozenRooms(floor.rooms)` 重新冻结」，于是画第二个房间的第一面墙时，快照从「画第一个房间前（空）」更新成「含第一个房间」，`bounds` 突变、视口跳到第一个房间中间。之前一直在查「画完闭合」的时机，其实跳的是「下一个房间的第一面墙」那一瞬间
- **修复**：`setFrozenRooms` 只在 `frozenRooms === null`（未冻结）时设一次，连续画多个房间一直用第一次的快照
- **教训**：取景冻结要「只冻一次、直到显式解冻」，不能每次进画墙草稿都重新冻；「跳转」的触发点不一定在用户以为的动作上（画完闭合），要顺着状态突变的时间点倒查

## 2026-08-15 23:30 · 墙多选不限条数 + 约束支持多条

- **问题**：`toggleWallSel` 用了 `.slice(-2)` 把多选卡死成最多 2 条；`applyWallConstraint` 用 `ids.length !== 2` 也只处理 2 条。用户反馈「多选失效、共线/水平应该支持很多线段」
- **修复**：①`toggleWallSel` 去掉 `.slice(-2)`，可选中任意多条墙；②`applyWallConstraint` 改成 `for (id of ids.slice(1))` 循环——第 1 条当基准不动，后面所有选中的墙逐个应用约束（水平/垂直/共线/同点都支持多条）
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-15 23:50 · 检测项开关 + 短墙检测删除

- **背景**：画 2D 户型时会出现很多 2cm 短墙（吸附/闭合产生的超短墙段），要能检测出来并删掉；检测项要能逐个开关
- **实现**：①store 加 `detectOpts`（zeroLen/shortWall/dup/overlap/dangling 开关 + shortLen 短墙阈值默认 0.05m）；②`detectWallIssues` 按开关逐项检测，加「短墙」检测（`0.01 <= len < shortLen`）；③`fixWallIssues` 删除时把 `shortWalls` 也纳入 removeIds；④PlanEditor 标注加「短墙」；⑤工具栏「检测」旁加「⚙」弹出面板（5 项 checkbox + 短墙阈值输入，单位 m）
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-16 00:05 · 检测设置面板改右侧 + 取消按钮

- **问题**：检测设置的 ⚙ 弹窗用 `bb-menu`（`bottom:100%` 向上弹），底部工具栏在屏幕底、向上弹超出屏幕顶，看不到内容
- **修复**：①删掉 bb-menu 浮层，改成右侧固定面板（`position:fixed; right:14; top:90`，detectOptsOpen 时显示），检测项 checkbox + 短墙阈值 + 「🔍 检测 / 🔧 修复 / 取消」三按钮；②加 `cancelDetect`（清 wallIssues + store.wallIssues，取消红色标注）
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-16 00:15 · 检测面板点击空白收回 + 伸出/孤立拆开

- **点击空白收回**：检测设置面板加 `data-detect-panel` 标记 + `useEffect` 监听 `mousedown`（`closest('[data-detect-panel]')` 命中就不关，否则关闭）。用户要求「以后所有弹窗默认点击空白处收回」，已记入反馈.md 铁律 10
- **伸出/孤立拆成两项**：检测里 `danglingIds` 原来合并「一端或两端不接」。拆成 `danglingIds`（伸出：一端接一端不接）+ `isolatedIds`（孤立：两端都不接）。store `detectOpts` 加 isolated 开关；`fixWallIssues` 把 isolatedIds 直接删、danglingIds 保持修剪；PlanEditor 标注加「孤立」
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-16 00:25 · 存档列表按修改时间排序（最新在前）

- **问题**：`/api/backups` 原来按文件名 `sorted(..., reverse=True)` 降序（文件名含时间戳，同项目名才正确）；项目名不同时按项目名字典序排、顺序乱，用户要求「最后保存的靠最上面」
- **修复**：改成按 `os.path.getmtime` 修改时间降序 `files.sort(key=lambda f: getmtime(...), reverse=True)`，最新保存的始终在最上面
- **验证**：部署 `{"ok":true}`；无头 `#root` 7724、0 错误 ✓

## 2026-08-16 00:35 · 批量墙颜色加调色盘

- **原版墙颜色**：`#d5e0f1`（浅蓝灰，毛玻璃半透明，wallOpacity 0.197），在 `WALL_COLORS` 里已有
- **改动**：批量墙（>2 面）面板的颜色行，保留 8 色 swatch 快捷色 + 加 `HexColorPicker`（六边形色盘 + 明度 + hex 输入 + 黑白灰 + 收藏），选任意颜色 `patchWalls({ color: c })` 批量改
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-16 00:55 · 主界面旋转失控/回弹（长期 bug 终于定位）

- **现象**：主界面左键旋转模型，视图偶尔不受控制地放大、或下拉又自动抬上去（技术笔记里的 bug 2，改了很多回没解决）
- **深挖排除**：①R3F 的 onClick 有 `delta <= 2` 像素阈值，拖拽旋转不会误触发「点房间跳视角」；②OrbitControls 的 onMouseWheel 里 `state !== NONE` 直接 return，旋转中忽略滚轮、不会误缩放。两个旧怀疑方向都排除
- **真根因**：`enableDamping=true` + `dampingFactor=0.08` 的**阻尼惯性**——松手后相机继续转约 1 秒，快拖再松手就「甩出去/不受控制放大」；叠加 `maxPolarAngle=87°` 极角钳制，往下拖到极限被顶回 →「下拉又自动抬上去」
- **修复**：①`enableDamping=false`（松手即停、跟手）；②极角放宽 `minPolarAngle=0.02`、`maxPolarAngle=Math.PI/2-0.02`（约 89°）
- **⚠️ 教训**：排查 bug 不能只猜「状态/重渲染」，要顺着事件链路逐条验证排除（click 阈值、wheel 的 state 拦截），最后落到 OrbitControls 自身的阻尼+钳制配置上。之前改 delta/指纹/太阳取整都是隔靴搔痒
- **验证**：build ✓；无头 `#root` 7723、0 错误 ✓；部署健康 ✓

## 2026-08-16 01:05 · 旋转后「突然放大」的二次根因（惯性滚动）

- **现象**：禁用阻尼后「乱甩乱弹」好了，但还残留「旋转松手突然放大」
- **根因**：OrbitControls 的 onMouseWheel 只在 `state !== NONE`（旋转进行中）忽略滚轮，但**松手瞬间**触控板/鼠标的惯性滚动（momentum wheel）会立刻触发 dolly → 旋转一松手就放大
- **修复**：Controls.js 加 useEffect，在 domElement 上 `pointerup` 后 200ms 内、wheel 事件用 capture 阶段（先于 OrbitControls 的 onMouseWheel）`stopPropagation + preventDefault`，挡掉惯性滚动的缩放。主动滚轮（停 0.2s 再滚）不受影响
- **验证**：build ✓；无头 `#root` 7621、0 错误 ✓；部署健康 ✓

## 2026-08-16 01:15 · 「突然放大」真正的根因（不是滚轮，是 click 误触发）—— 已撤销上面 01:05 的错误修复

- **用户质疑**：只用鼠标左键，为什么触发滚轮？→ 说明 01:05 的「惯性滚动」方向错了
- **真根因**：深挖 R3F 源码（events）确认——R3F 的 `onClick` 对「有命中对象」的情况**不做 delta 过滤**（`delta <= 2` 只在点空白处 pointerMissed 分支里）。所以左键旋转松手时只要鼠标还停在房间/家具上，就触发 onClick → `handleSelect` 点房间跳视角（camViewSignal 飞到房间中心、camDist 变小 = 视觉「突然放大」）。只用左键，与滚轮无关
- **修复**：①Scene.jsx 加 `isDragClick(e) = e.delta > 6`，给 Room/Furniture/Device/Opening 的 onClick 统一开头 `if (isDragClick(e)) return`（拖拽旋转松手不算点击）；②撤销 01:05 的 wheel 拦截（错误假设，且有副作用）
- **⚠️ 教训（血泪）**：①不能凭「现象像滚轮缩放」就猜是 wheel——用户一句「只用左键」直接推翻假设；②要读三方库源码验证事件过滤逻辑，R3F 的 click delta 过滤只覆盖 pointerMissed，不覆盖命中对象的 onClick；③原版 JMGLink 丝滑的关键就是拖拽/点击分得清
- **验证**：build ✓；无头 `#root` 7621、0 错误 ✓；部署健康 ✓

## 2026-08-16 01:25 · 恢复阻尼（丝滑感），对齐原版 JMGLink

- **用户反馈**：放大修好了，但旋转「很硬」，没有原版那种丝滑滑动感。之前开阻尼会乱跳，但原版「丝滑又不乱跳」
- **查原版代码**：3d-home-addon 的 app.js 里 `controls.enableDamping = true; controls.dampingFactor = 0.08; controls.update()`（不传 delta）——**和之前我们的一模一样**。说明「丝滑滑动感」正是阻尼本身
- **关键教训（血泪）**：之前把「乱甩」误归因于阻尼惯性，一刀切禁用 damping，结果把丝滑感也误伤了。真凶其实是 click 误触发跳视角（已修）+ 极角钳制（已放宽），**不是阻尼**。排查要分清「阻尼惯性（丝滑）」和「跳视角/钳制（乱跳）」两个独立现象，不能一起误杀
- **修复**：恢复 `enableDamping=true` + `dampingFactor=0.08`（对齐原版），保留极角放宽 89° + isDragClick 过滤
- **验证**：build ✓；无头 `#root` 7621、0 错误 ✓；部署健康 ✓

## 2026-08-16 01:40 · 工具栏「房间」按钮 + 左侧房间导航面板

- **需求**：主页工具栏加按钮，点开左侧弹出房间切换面板（全部 + 各房间），点房间跳转居中，参照原版 JMGLink 楼层切换原理和 UI 布局
- **实现**：①store 加 `roomNavOpen`；②BottomBar 加 `room` 按钮（`room-btn`，DEFAULT_ORDER 放「居中」后）；③App.jsx 加 `focusRoom`（算房间包围盒中心 + `camTarget/camDist/camViewSignal` 跳转，复用 handleSelect 点房间公式）和 `focusAll`（`recenterKey+1` 触发 CameraFocus 重取景）+ 左侧 `.room-nav` 面板（全部 + 各房间）；④styles.css 加 `.room-nav`（position:fixed left）；⑤点空白处收起（复用 notifOpen 的 mousedown 模式）
- **验证**：build ✓；无头 `#root` 7621、0 错误 ✓；部署健康 ✓

## 2026-08-16 01:45 · 修复「房间」按钮不显示（自定义顺序覆盖）

- **问题**：加的房间按钮只在 `DEFAULT_ORDER` 里，但用户之前自定义过工具栏顺序（`settings.toolbarOrder`），`order` 用的是自定义顺序，新按钮没进去 → 界面不显示
- **修复**：`order` 计算改成「以自定义顺序为基础，把 DEFAULT_ORDER 里缺失的新按钮按相对位置补插进去」（通用：以后加新按钮也会自动补）
- **⚠️ 教训**：给「可自定义顺序/可隐藏」的列表加新项时，不能只改默认配置，要处理「用户已保存自定义配置」的情况，否则新项被旧配置吞掉不显示
- **验证**：build ✓；无头 `#root` 8107、0 错误 ✓；部署健康 ✓

## 2026-08-16 01:55 · 房间切换改成「单房间隔离显示」+ UI 对齐原版

- **用户纠正**：之前做的是「相机飞到房间中心但整个户型还在」，用户要的是「选中房间后其他房间全部隐藏、整个屏幕只显示这一个房间」；且 UI 太丑，要对齐原版
- **实现**：①store 加 `focusRoomId`（null=全部，非空=只显示该房间）；②Scene.jsx 加过滤——房间 `filter(r => !focusRoom || r.id === focusRoomId)`、墙 `filter(wallInFocus)`（两端点都在聚焦房间边界上，用 pointToSeg dist<0.08）、家具/设备 `filter(ptInFocus)`（pos 在房间多边形内，pointInPolygon）；③App.jsx `focusRoom` 设 `focusRoomId` + 相机跳转，`focusAll` 清 `focusRoomId` + recenterKey；④进入编辑/切 2D 时 `useEffect` 重置 focusRoomId；⑤UI 对齐原版（`.room-nav` 用 var(--panel) + group-label 小字 uppercase + floor-item 风格列表 + active 蓝底描边）
- **⚠️ 教训**：①「跳转居中」≠「只显示该房间」，用户要的是单房间隔离（隐藏其他），要听清语义；②参照原版 UI 要真的去看它的 CSS（sidebar/group-label/floor-item），不是自己拍脑袋写
- **验证**：build ✓；无头 `#root` 8107、0 错误 ✓；部署健康 ✓

## 2026-08-16 02:05 · 房间导航三处修正（放大一致/透明UI/点击空白收回）

- **放大倍数一致**：`dist = Math.max(5, 宽*1.6, 高*1.8)` 的下限 5 会把小房间的相机距离钳死、占屏比例和大房间不一致（用户说小房间一放大就超出屏幕）。改成下限 2.5（同时改 handleSelect 点房间和 focusRoom 两处），所有房间占屏比例统一
- **UI 透明**：`.room-nav` 从 `background:var(--panel)`（深色块）改成 `background:transparent; border:none`，列表项半透明 `rgba(255,255,255,0.04)`，对齐右侧 side-panel 的透明效果（用户说「人家是透明效果，你做那么黑」）
- **点击空白收回**：原 `document.addEventListener('mousedown')` 没生效（被 Canvas 的 OrbitControls/R3F 拦截），改成 `window.addEventListener('pointerdown', onDown, true)`（capture 阶段先于拦截）
- **⚠️ 教训**：①`Math.max(下限,...)` 的下限是「缩放一致性」的破坏者，小房间会被钳到固定距离；②3D Canvas 上的全局点击监听要用 pointerdown+capture，mousedown 会被 OrbitControls/R3F 吞掉；③「透明效果」= 面板 transparent + item 半透明 rgba，不是深色 panel
- **验证**：build ✓；无头 `#root` 8107、0 错误 ✓；部署健康 ✓

## 2026-08-16 02:15 · 房间弹窗逻辑/图标/样式最终对齐原版

- **用户纠正（生气）**：①房间弹窗逻辑要和场景弹窗一样——点空白/点别的按钮**都不收回**，只有点「房间」按钮本身 toggle（我之前加「点击空白收回」又理解反了）；②小房子 emoji 换成线条图标；③「改透明但效果不对」——原版 floor-item 是默认 `border:1px solid transparent` + `background:transparent`，hover 才微亮，active 蓝底+蓝描边，我之前默认就给了边框+半透明背景
- **修复**：①删掉 roomNavOpen 的点击空白收回 useEffect；②「全部」用四宫格线图、房间用单方块线图（SVG stroke）；③room-nav-item 默认 transparent 边框+透明背景，hover `rgba(255,255,255,.04)`，active `rgba(90,162,255,.15)`+accent 描边
- **⚠️ 教训**：用户说「和 X 一样」要原样照抄 X 的行为和 CSS，不能自作主张加「点击空白收回」这类额外逻辑；emoji 表情要换成统一 stroke 线图
- **验证**：build ✓；无头 `#root` 8107、0 错误 ✓；部署健康 ✓

## 2026-08-16 02:25 · 房间聚焦补上 3D 高度（不超出屏幕）

- **问题**：小房间聚焦时超出屏幕——dist 只按地板宽高算（`Math.max(2.5, 宽*1.6, 高*1.8)`），没算房间的 3D 高度（2.8m）。小房间地板小、相机近，但墙有 2.8m 高，垂直方向就顶出屏幕
- **修复**：focusRoom 和 handleSelect 的 dist 都加上 `roomH * 1.3`（roomH = room.height || floor.height || 2.8），对齐原版 JMGLink 的 `floorH * 1.3`
- **⚠️ 教训**：3D 取景要同时按「水平包围盒 + 垂直高度」算距离，只算地板宽高会漏掉墙的高度、导致小房间超出屏幕
- **验证**：build ✓；无头 `#root` 8107、0 错误 ✓；部署健康 ✓

## 2026-08-16 02:40 · 房间「锁定视角」功能（每房间保存视角，持久化）

- **需求**：用户对自动居中的缩放不满意，要每个房间能手动锁定视角（放大比例+角度），下次点房间用锁定的视角、且能保存
- **实现**：①geometry.js `recomputeRooms` 保留 `lockedView`（{pos,target}）；②store 加 `roomView`（要应用的锁定视角）；③Controls.js `viewSignal` 加 `'roomview'` 分支（读 roomView 设 camera position/target）；④App.jsx `focusRoom` 先查 `room.lockedView`，有则走 roomview、无则默认居中；`lockRoomView` 存 `window.__cam3d.pos/target` 到 `room.lockedView`（setState 触发保存）；⑤房间面板每行加锁按钮（`.room-lock`，hover 显示、locked 常显 accent 色）
- **⚠️ 教训**：用户对「自动计算」失去信任时，给「手动覆盖」的入口（锁定视角）是更快的解法，不用硬调自动公式
- **验证**：build ✓；无头 `#root` 12642、0 错误 ✓；部署健康 ✓

## 2026-08-16 02:55 · 单房间聚焦漏墙 + 「全部」偏移

- **漏墙（卫生间少墙/厨房少地板）**：`wallInFocus` 用「两端点都在房间边界」判断，贯穿墙（一条墙跨多个房间、端点在房间外）的中间段才是聚焦房间边界，被误删。改成「墙与房间某条边共线且交叠」判断（w 端点在边上、或边的端点在 w 上），贯穿墙正确保留
- **「全部」偏移**：CameraFocus 的取景 box 把伸出墙也算进去，中心被带偏。改成「有房间时只按房间 points 取景、不含墙；没房间才按墙取景」
- **⚠️ 教训**：①墙「属于」房间要用「共线交叠」判断，不能用「两端点都在边界」（贯穿墙会漏）；②「居中取景」要排除伸出墙/悬空对象，只按房间包围盒算中心
- **验证**：build ✓；无头 `#root` 12709、0 错误 ✓；部署健康 ✓

## 2026-08-16 03:10 · 墙过滤改「覆盖边」+ 自动修剪伸出墙

- **问题**：①之前「共线交叠」判断太宽松，把沿房间边延伸的伸出墙也误判进房间（更乱）；②户型有很多伸出墙，干扰房间检测；③「地板不显示」其实是透明度太高（用户自己发现，非 bug）
- **修复**：①wallInFocus 改成「墙完整覆盖房间某条边」（边的两个端点都落在墙上）——贯穿墙保留、伸出墙排除；②geometry.js 加 `autoTrimWalls`：一端悬空缩回最近交点、两端悬空删除；③PlanEditor 的 closeDraft 和 addWallPoint 闭合后自动调 `autoTrimWalls`（画墙完成即自动清理伸出墙）
- **⚠️ 教训**：①「共线交叠」会误判伸出墙，「覆盖整条边」才是区分贯穿墙/伸出墙的正确标准；②自动优化要放在「画墙完成」时机（草稿墙画到一半是悬空的，不能修剪）；③用户反馈「地板不显示」先问一句透明度/显示问题，别急着改几何
- **验证**：build ✓；无头 `#root` 12709、0 错误 ✓；部署健康 ✓

## 2026-08-16 03:25 · 单房间聚焦的墙改为「用房间边界直接生成」（用户点破的正确思路）

- **用户纠正（关键）**：别去过滤原始墙（贯穿墙/伸出墙各种边界情况搞不清），直接用「房间地板边界（room.points）重新生成墙」——每个房间多边形的每条边就是一面墙，不多不少
- **实现**：Scene.jsx 墙渲染处，`focusRoom` 非空时直接用 `focusRoom.points` 的每条边生成 `boxGeometry` 墙（高度 room.height、厚度 WALL_THICK），不再走 `floor.walls` 过滤；删掉 wallInFocus（不再需要）。显示全部时仍正常渲染原始墙
- **⚠️ 教训（血泪）**：房间的墙 = 房间边界（room.points），这是 detectRooms 已经算好的精确结果。单房间显示时直接用边界生成墙，比「从原始墙里判断哪条属于房间」简单正确 100 倍——之前绕了一大圈（两端点/共线交叠/覆盖边）都是舍近求远
- **验证**：build ✓；无头 `#root` 12709、0 错误 ✓；部署健康 ✓

## 2026-08-16 03:35 · 聚焦房间墙参数对齐原始墙

- **用户纠正**：聚焦房间用边界生成的墙，参数（尤其透明度）和用户设置的墙不一致——用户用透明墙，我写死了 opacity=1 不透明
- **修复**：聚焦房间的每条边，用 `matchWall(a,b)` 从 floor.walls 找「覆盖这条边」的原始墙，用它的 thickness/height/color/opacity；找不到才用默认（height=room.height、thick=WALL_THICK、color=th.wallColor、opacity=100）
- **⚠️ 教训**：用边界「重新生成」对象时，别写死默认参数——要回头匹配原始对象，继承它用户已设置好的参数（透明度/厚度/颜色/高度）
- **验证**：build ✓；无头 `#root` 12709、0 错误 ✓；部署健康 ✓

## 2026-08-16 03:50 · 房间导航栏加「编辑」按钮（改颜色/透明度）

- **需求**：房间导航栏每个房间加编辑按钮，点开编辑该房间详细参数——只能改颜色/透明度，尺寸不改
- **实现**：①store 加 `roomEditId`；②App.jsx 加 `patchRoom`（改 room.color/opacity）、`patchRoomWalls`（找房间边界对应的原始墙统一改 color）、`wallColorOf`（取第一条边界墙颜色作色盘初始值）、`editRoom`；③房间行加铅笔编辑按钮；④`roomEditId && editingRoom` 弹 modal：地板颜色（HexColorPicker）+ 地板透明度（range）+ 墙壁颜色（HexColorPicker）
- **验证**：build ✓；无头 `#root` 14497、0 错误 ✓；部署健康 ✓

## 2026-08-16 04:05 · 房间编辑改成进 2D 编辑 + 「互不互通」开关

- **用户纠正**：点铅笔不是弹窗，要「进入 2D 编辑界面」编辑每个墙；左侧导航面板旁加「互不互通」开关——控制改单个房间的墙颜色/透明度/地板颜色是只改自己、还是整个户型一起改（家具摆放不包含）
- **实现**：①`editRoom` 改成 `setState({ editing:true, view2d:true, selected:{type:'room',ref:room}, focusRoomId:null, roomNavOpen:false, roomEditId:room.id })`（进 2D 编辑选中房间，右侧属性面板改色）；②删掉 modal 弹窗；③store 加 `roomSync`；④导航面板「房间」标题下加「互通/独立」开关；⑤PlanEditor 的 `patchRoom`/`patchWall`/`patchWalls` 都认 `roomSync`——开启时改所有房间/所有墙，否则只改当前
- **⚠️ 教训**：改动要落在「实际修改数据的地方」（PlanEditor 的 patch 函数），不是只在入口处做文章
- **验证**：build ✓；无头 `#root` 14865、0 错误 ✓；部署健康 ✓

## 2026-08-16 09:26 · 单房间 2D 编辑（真正只显示该房间）+ 退出编辑聚焦到该房间 + 独立/互通按钮进工具栏

- **需求（用户明确）**：①点房间「编辑」进 2D 后，要只显示**这个房间的平面图**（不是整个户型）；②退出编辑要退到「3D 聚焦这个房间」的界面，不是整个户型；③进 2D 编辑后还能切「独立/互通」
- **问题根因**：上一版 `editRoom` 设了 `roomEditId`，但 PlanEditor 根本没用它做过滤渲染 → 进 2D 还是整个户型；且「进编辑就清 focusRoomId」的 useEffect 把单房间聚焦一进编辑就清掉了
- **实现**：
  1. PlanEditor 加单房间聚焦：`editingRoom = roomEditId ? rooms.find(id) : null`；房间/墙/门窗/家具/设备/交点全部按它过滤（墙=覆盖房间某条边 `pointToSeg` 双端点落在边上；家具设备= `pointInPolygon`）；取景 bounds 改按 `editingRoom.points` 包围盒（pad 1.5m 紧凑居中）
  2. 退出编辑把 `roomEditId` 转移到 `focusRoomId`（函数式 `setState(s=>({focusRoomId:s.roomEditId, roomEditId:null}))`），3D 主界面就聚焦这个房间
  3. `editRoom` 同时设 `focusRoomId=room.id`（编辑期间切 3D 也聚焦），删掉「进编辑清 focusRoomId」的 useEffect（旧假设「编辑要看全部」被推翻）
  4. 独立/互通开关加到 Editor 工具栏（原来只在左侧 room-nav 面板，进 2D 后面板关了看不到）
- **⚠️ 教训**：①「单房间显示」= 用 roomEditId 做渲染过滤，别只设 state 不消费；②退出/切视图时「聚焦」要转移（roomEditId→focusRoomId），不是直接清 null；③两个聚焦机制（2D 用 roomEditId、3D 用 focusRoomId）要协同，点编辑时一起设
- **验证**：build ✓；无头实测——点房间编辑后 `.plan-room=1`、`.plan-wall=3`（该房间边数）、退出后该房间 active（3D 聚焦）、右侧属性面板「名字/面积/尺寸锁定/长/宽/高度/厚度/颜色/透明度/壁纸」齐全、0 错误 ✓；部署健康 ✓

## 2026-08-16 09:38 · 修复「改墙颜色/透明度没生效」+ 编辑作用域区分（房间 vs 整个户型）

- **用户报告**：房间 2D 编辑里选中所有墙改颜色/透明度，退出编辑后没生效；且「全选墙」选的是整个户型的墙（不是这个房间的），点「独立」按钮也不管用
- **根因 1（store 重渲染，铁律级）**：`patchWalls`/`patchRoom` 用 `Object.assign` **原地改**墙/房间对象，然后 `setState({project:{...st.project}})` 只浅拷贝 project、`floors` 数组和 `floor` 对象引用都不变。`useStore` 用 `useSyncExternalStore` + `Object.is` 比较 selector 返回的 `floor` 对象，引用没变 → **组件不重渲染** → 数据其实保存了（project.json 里 color 已变），但界面不刷新，看起来「没改成功」
- **修复 1**：store.js `setState` 里，project 变化时浅拷贝每层 floor（`next.project = {...next.project, floors: floors.map(f=>({...f}))}`），floor 引用变了触发依赖 floor 的组件重渲染
- **根因 2（作用域没区分）**：`selectAllWalls`/`selectAllRooms` 无论从哪进编辑都选整个楼层的墙/房间；房间进编辑时应该只选这个房间
- **修复 2（作用域）**：①Editor.jsx `selectAllWalls`——roomEditId 非空时只选「覆盖这个房间某条边」的墙（pointToSeg 双端点落在边上），否则选全部；`selectAllRooms`——roomEditId 非空时只选 [roomEditId]，否则选全部；②PlanEditor 的 `patchRoom/patchWall/patchWalls/patchRooms` 在房间作用域（roomEditId 非空）下忽略 roomSync（互通不波及整个户型）；③2D 墙 stroke 改用 `w.color`（之前用 CSS 固定色 #343a3f，改颜色 2D 看不到）
- **⚠️ 教训（血泪）**：①**原地改 store 里的对象，必须让引用变化触发重渲染**——`useSyncExternalStore` 只认引用（Object.is），浅拷贝 project 不够，要连 floor 一起浅拷贝；②「全选」要跟着「作用域」走（房间编辑只选房间、户型编辑才选全部），不能一刀切选整个楼层；③「互通/独立」这类全局开关，在房间作用域下要让位（房间进编辑就是只改房间）
- **验证**：无头实测——房间作用域全选墙选中 3 面（非 20）、改浅绿 stroke 立即变 #d0e8d8、改回白 #ffffff、0 错误 ✓；部署健康 ✓

## 2026-08-16 09:43 · 修「四面墙只选三面」（拼接墙误判）+ 房间作用域隐藏无用按钮

- **问题 1**：房间明明四面封闭，全选墙只选中三面。根因：这个房间的右边墙是**两段墙拼接**（上半段 `6l5wj1ke` + 下半段 `sms2hv4f`，在 y≈-3.055 处相接），而 `wallCoversRoomEdge`/`selectAllWalls` 用「单面墙完整覆盖房间某条边」（两端点分别落在某条边的两个顶点上）判断，两段墙都没有一面能完整覆盖整条边 → 右边墙被误判「不属于房间」→ 2D 缺墙、全选漏选
- **修复**：判断改成「墙的两个端点都落在房间边界上（pointToSeg dist<0.08）」就算属于这个房间，不要求单面覆盖整条边。改了 PlanEditor `wallCoversRoomEdge` 和 Editor `selectAllWalls` 的 covers 两处
- **问题 2**：房间作用域（从房间「编辑」进）下工具栏一堆画户型/放家具的按钮没用，要求隐藏
- **修复**：用 roomEditId 判断，房间作用域下隐藏——楼层管理/导入底图/标定/底图缩放/清空全部/模型/设备/绑定设备/房间模板/全部锁定解锁/检测修复/吸附精度/独立互通 + 左侧工具只留「浏览/选择」。保留：项目/撤销/重做/默认/背景/保存/缩放居中/标签/名字/尺寸/交点/全选墙/全选地板/2D/3D/多选
- **⚠️ 教训**：①房间的边 ≠ 一面墙，可能是多面墙拼接——判断「墙属于房间」要看「端点都在边界上」，别用「单面覆盖整条边」；②「作用域」不仅是数据操作（全选/patch），还包括 UI——房间编辑就该只显示改色相关的按钮
- **验证**：无头实测——房间作用域墙数 5（含右边两段）、全选墙选 5 面、无用按钮全隐藏、0 错误 ✓；部署健康 ✓

## 2026-08-16 10:07 · 3D 聚焦视图拼接墙继承颜色修复（matchWall）

- **用户报告**：房间编辑里全选墙改颜色/透明度，退出编辑后 3D 里「没保存」（实际数据已保存，但 3D 聚焦视图右边拼接墙用了默认色，看起来像没改成功）
- **根因**：3D 聚焦视图的 `matchWall`（Scene.jsx）还是旧判断「单面墙覆盖整条边」（房间边两个顶点都落在墙上），拼接墙（右边两段）匹配不到 → `wallColor`/`wallOpacityVal` 落到默认值
- **修复**：`matchWall` 改成「墙的两个端点都落在房间这条边上」（`pointToSeg(w.start,a,b)<0.08 && pointToSeg(w.end,a,b)<0.08`），和 2D 的 `wallCoversRoomEdge` 一致，拼接墙每段都能匹配、继承用户设的颜色/透明度
- **验证**：python 实测——多功能室 4 条边全部匹配到墙（右边边2 匹配 2 段、color 都是 #ffffff）；无头全流程 0 错误 ✓；部署健康 ✓

## 2026-08-16 11:02 · 共用墙继承颜色修复（wallOnEdge 统一三种墙）

- **用户报告**：卫生间有一面墙是「白色不透明」，而户型图其他墙是透明的（opacity 10）
- **根因**：卫生间的上边墙是「共用墙」（= 多功能室的下边墙），它延伸到 -5.69，比卫生间的边（到 -4.797）更长。之前「墙端点都在房间边上」判断用 `pointToSeg`（会 clamp 到线段端点），共用墙端点超出边 → 距离被算成 0.89m → 匹配不到 → 3D 聚焦视图用默认色 + 不透明
- **修复**：geometry.js 新增 `wallOnEdge(w, a, b)` 统一判断「墙是否构成房间边的一部分」：①共线用「到直线的垂直距离」（不能用 pointToSeg，它 clamp 会把端点超出的共用墙误判）②交叠用投影区间重叠 ③最小重叠长度 >5cm（排除吸附误差产生的几毫米假交叠）。替换 PlanEditor `wallCoversRoomEdge`、Editor `selectAllWalls`、Scene `matchWall` 三处
- **⚠️ 教训（血泪）**：判断「墙属于房间边」要覆盖三种情况——①单面墙=整条边 ②拼接墙（墙是边的一段，端点都在边上）③共用墙（墙覆盖边并延伸，端点超出边）。前两次分别只覆盖了前两种、各漏一种；「共线」必须用「到直线距离」而不是「点到线段距离」（后者 clamp 会把共用墙误判成不共线）
- **验证**：python 全量实测——6 房间墙匹配全部正确（多功能室5/阳台4/厨房4/卫生间4/客厅6/卧室5，多出的都是拼接墙正常多段）；卫生间进编辑墙数 4、全部 #ffffff+opacity0.1、0 错误 ✓；部署健康 ✓
- **待办（未做）**：阳台有 1 条 5cm 的「误差短边」（detectRooms 吸附残留，数据里没有对应墙），不影响使用，后续可优化 detectRooms 清掉这类假边

## 2026-08-16 11:09 · 从源头杜绝拼接墙：画墙后自动合并共线墙

- **用户要求**：不要靠事后匹配判断补救，要从「编辑的时候就杜绝」拼接墙这种数据问题（以后别的用户画也会遇到）
- **实现**：geometry.js 新增 `mergeCollinearWalls(walls)` —— 把「共线且相连/重叠」的墙段合并成一面（反复直到无合并）。判断：4 端点互相到对方直线距离 < SNAP_TOL（共线）+ 投影区间相连/重叠。画墙完成（closeDraft 和 addWallPoint 闭合分支）时先 mergeCollinearWalls → autoTrimWalls → recomputeRooms
- **效果**：真实户型 20 面墙 → 12 面（合并 8 面拼接墙），房间结构 6 房间不变
- **也帮用户清理了当前数据**：备份后 POST 合并结果回后端，现在 project.json 是 12 面墙 + 6 房间
- **⚠️ 教训**：数据类 bug 要「从源头杜绝」（在产生数据的时机规范化：画墙后合并/吸附/清理），不能只在消费侧（渲染/匹配）打补丁——消费侧补丁只能救当前数据，救不了未来用户新画的数据
- **验证**：合成数据测试（拼接合并/不相连不合并/垂直不合并全对）+ 真实数据测试（20→12、6 房间不变）+ 无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 11:58 · 「独立/互通」改成房间级独立颜色（房间 ↔ 户型图断开）

- **用户澄清语义**：「独立/互通」不是房间和房间（房间间本来就不互通），而是「房间 ↔ 户型图」——某个房间设为「独立」后，总体编辑（整个户型）改墙/地板颜色时，这个房间保持自己的颜色不动
- **重构**：
  1. 房间对象加 `independent` 字段（recomputeRooms 保留）
  2. 删掉全局 `roomSync`（之前的语义「改单个 vs 全部」理解错了）
  3. room-nav 面板「独立/互通」按钮改成作用于**当前聚焦房间**（focusRoomId）：点房间聚焦 → 按钮显示该房间状态 → 点它切换 independent
  4. `patchWalls`/`patchRooms`（批量改色）跳过 independent 房间的墙/地板；`patchWall`/`patchRoom`（单个改）不受影响（单独点这面墙/这个房间就是要改它）
  5. 房间属性面板加「颜色独立」开关（跟「尺寸锁定」并列）
- **⚠️ 教训**：①「独立」这类词要问清作用对象——是「房间↔房间」还是「房间↔户型图」，两者实现完全不同；②批量改色（全选墙）要跳过独立房间的边界墙（用 `wallOnEdge` 判断墙是否属于独立房间），但单个改色不跳过（用户明确点这面墙就是要改）
- **验证**：无头实测——多功能室设独立后，总体编辑全选改浅绿，多功能室 5 面保持 #ffffff、其余 7 面变 #d0e8d8、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:08 · 修复「全部」按钮视角不居中（OrbitControls target 没同步）

- **用户报告**：点房间名聚焦后，再点「全部」，显示的户型视角位置不固定/不居中，应该一直居中
- **根因**：`focusAll` 只设 `recenterKey+1` 触发 CameraFocus 重取景。CameraFocus 只 `setState({camTarget})` + `camera.position.set` + `camera.lookAt`，**不更新 OrbitControls 的 target**（旋转中心）。而 Controls 每帧 `useFrame` 调 `OrbitControls.update`，会用「旧 target」（之前聚焦的房间中心）重新把相机朝向拽回房间中心 → 相机位置居中但朝向偏了，户型图不居中
- **修复**：`focusAll` 改成跟 `focusRoom` 一样——**直接算整个户型的包围盒中心，设 `camTarget` + `camViewSignal`**（不再靠 recenterKey）。这样 Controls 的 viewSignal useEffect（type='default'）会同步 `target.set(户型中心)` + 设置相机，target 正确更新
- **⚠️ 教训（血泪）**：相机「居中」不止要设 `camera.position` 和 `lookAt`，还要同步 OrbitControls 的 **target**（旋转中心）——OrbitControls 每帧 update 会用 target 覆盖相机朝向。走 `camViewSignal` 的路径会设 target，走 `recenterKey`（CameraFocus）的路径不会，两者要分清
- **验证**：无头实测——点房间后 target=[-3.94,-2.55]（房间中心），点全部后 target 回到 [-0.94,-3.18]（户型中心），连续 5 次全部居中 5/5、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:24 · 「独立/互通」最终语义：每个房间两套独立配色（房间单独视图 vs 户型图）

- **用户澄清（第三次）**：不是「总体编辑跳过独立房间」，而是「每个房间单独一份纯存储数据」——房间编辑里改的墙/地板颜色，只存在房间自己的数据里，「全部」视图里不显示；「全部」视图显示的是「在全部里给房间定义的颜色」，两套完全独立
- **重构**：
  1. 房间对象加 `soloWallColor/soloWallOpacity/soloFloorColor/soloFloorOpacity`（房间独立配色，undefined=跟随户型图），recomputeRooms 保留
  2. PlanEditor patch 分作用域：房间作用域（roomEditId 非空）下 `applyRoomPatch` 把 color/opacity 路由到 solo 字段、几何字段（height/thickness）照旧改对象；户型作用域照旧改 w.color/room.color
  3. 渲染分作用域：2D 墙 stroke、3D 聚焦墙（focusRoom.soloWallColor）、地板（isFocused ? soloFloorColor）在房间单独视图用 solo 优先；全部视图用 w.color/room.color
  4. 删掉之前的 independent 字段 + toggleRoomIndependent + room-nav「独立/互通」按钮 + 房间面板「颜色独立」开关（两套天然独立，不需要按钮）
- **⚠️ 教训**：「独立/互通」这类需求要反复确认作用对象和方向——第一版理解成「房间↔房间」，第二版「总体编辑跳过独立房间」，第三版才理解成「每个房间两套独立配色（房间视图 vs 户型图）」。语义分歧大时先复述确认再动手
- **验证**：无头实测——房间编辑改浅绿→2D/3D 房间视图显示浅绿；点「全部」后浅绿消失、显示白色户型图配色（8 个白墙）、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:32 · 地板颜色统一成「快捷色 + 色盘」

- **用户反馈**：地板颜色没有色盘选择，只有固定几种颜色，包括全选地板面板
- **现状**：单个房间地板只有 HexColorPicker（色盘）没快捷色；全选地板（批量房间）只有 FLOOR_COLORS 固定 swatch 没色盘
- **修复**：两处都统一成「快捷色 swatch + HexColorPicker 色盘」——单个房间地板（selRoom 面板）加 8 个 FLOOR_COLORS 快捷色；全选地板（批量房间面板）加 HexColorPicker。跟批量墙面板保持一致
- **验证**：无头实测——全选地板面板 8 个快捷色 + 1 个色盘 hex 输入框、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:37 · 旋转速度上限配置 + 全选地板加厚度

- **旋转速度上限**：设置「默认选项」加「速度上限」输入框（`settings.rotateSpeedMax`，默认 5、max 100）；底部工具栏旋转速度滑动条的 `max` 改用 `settings.rotateSpeedMax || 5`，设上限后滑动条范围跟着变
- **地板厚度**：全选地板（批量房间）面板加「厚度」NumInput（`patchRooms({ thickness })`，默认 0.05m），跟单个房间地板一致
- **验证**：设置面板「速度上限」输入框 max=100 默认 5；全选地板面板 labels 含「颜色/透明度/厚度/贴图」、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:52 · 地板厚度 3D 渲染 + 合并重复按钮（模型/设备）

- **地板厚度没生效**：`room.thickness` 之前 3D 里完全没用——地板是零厚度平面（robustFloorGeometry 三角剖分），改了数据 3D 看不到
- **修复**：robustFloorGeometry 加 thickness 参数，>0.001 时用 `THREE.Shape + ExtrudeGeometry` 挤出薄板，`rotateX(90°)` 让上表面法线朝 +Y、厚度向 -Y（向下），顶面 y=0；Scene 传 `room.thickness || 0.05`
- **合并重复按钮**：左侧「家具」和顶部「模型」都是打开家具库（furnOpen），顶部「设备」「绑定设备」和左侧「设备」都是放设备（bindOpen）。合并成左侧两个按钮：`家具→模型`（setTool('furniture')→furnOpen）、`设备`（setTool('device')→bindOpen）；删顶部「模型/设备/绑定设备」三个按钮 + 设备分类面板（catOpen）
- **⚠️ 教训**：①「改数据没生效」先查渲染层有没有真正消费这个字段（thickness 只存不渲染=白改）；②功能按钮要做「去重」——先画清「哪个按钮打开哪个面板」，重复的合并到入口更少的那侧；③用户说「不是 X」= 之前理解错了，把 X 的改动撤销掉
- **验证**：robustFloorGeometry 0.2 厚度 bbox 高度 0.2、y∈[-0.2,0]（向下）；左侧「模型/设备」在、顶部三重复按钮无、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:55 · 「模型」两个弹窗都改右侧固定

- **用户澄清**：不是预览弹窗居中，而是「整个模型的弹窗」都放最右侧——点「模型」打开的家具库面板 + 点家具后的预览弹窗，都要右侧显示
- **修复**：①`.furn-picker`（家具库面板）从 `absolute; left:50% translateX(-50%)`（顶部居中）改成 `fixed; top:60px; right:14px`（右侧固定）；②家具预览弹窗 `.modal-mask.preview-right` 恢复（`justify-content:flex-end; align-items:flex-start; padding 60px 14px 0 0`，右对齐）
- **验证**：无头实测家具库面板 position=fixed right=14px、0 错误 ✓；部署健康 ✓

## 2026-08-16 12:59 · 家具库打开时隐藏「户型信息」面板（防重叠）

- **问题**：家具库面板改成右侧固定后，和「户型信息」面板（.editor-info，也是右侧）重叠了——右侧弹窗同时出现时户型信息该消失
- **修复**：户型信息面板的显示条件加 `!furnOpen`（家具库打开时隐藏）。原来条件：`!selected && !['wall','door','window'].includes(tool) && wallSelIds.length===0`
- **⚠️ 教训**：右侧弹窗/面板（户型信息/家具库/属性面板/设备抽屉）都占同一块区域，新增右侧面板时要同步检查「互斥显示条件」，别让两个右侧面板同时出现
- **验证**：无头实测——初始户型信息 1 个、点模型后家具库 1 个 + 户型信息 0 个、0 错误 ✓；部署健康 ✓

## 2026-08-16 13:06 · 房间弹窗只作用主界面 + 3D 编辑背景记忆

- **房间弹窗 scope**：room-nav 面板之前只在 `!view2d` 时隐藏，进 3D 编辑（view2d=false、editing=true）还会显示。修复：渲染条件加 `!editing`（编辑模式不显示）；底部「编辑」按钮进入编辑时加 `roomNavOpen:false`（退出编辑后面板不残留弹出）
- **3D 编辑背景记忆**：`editorBgImage/editorBgMode` 之前只 setState、没存 settings，刷新丢。修复：Editor.jsx 加 `saveEditorBg(image,mode)`（setState + api.saveSettings），背景弹窗 5 处改动全走它；App.jsx settings 加载处补 `editorBgImage/editorBgMode`
- **⚠️ 教训**：①弹窗/面板要明确「作用域」（只在哪个界面显示），跨界面（主界面↔编辑器）要加互斥条件；②「有记忆/下次还这样」= 要存 settings（或后端），不能只 setState——setState 刷新就丢
- **验证**：无头实测——主界面房间弹窗 1、进 2D/3D 编辑后 0、0 错误 ✓；部署健康 ✓

## 2026-08-16 13:20 · 太阳光开关

- **用户反馈**：关了「泛光」还是有反光，怀疑是太阳光（sun.sun 日照）
- **根因**：主界面有「太阳光」= `directionalLight`（方向光），位置随 sunElevation 变、intensity 1.6、castShadow。它跟「泛光 Bloom」（后处理光晕）是两码事，关泛光不影响太阳光
- **实现**：store 加 `sunLight:true`；App.jsx settings 加载 + 设置「默认选项」加 `{toggle('太阳光', settings.sunLight !== false, 'sunLight')}`；Scene.jsx directionalLight 包 `{sunLight && ...}`
- **⚠️ 教训**：光照分几套——环境光/半球光（基础照亮）、方向光（太阳光，有阴影）、泛光 Bloom（后处理光晕）、阴影（投影）。用户说「反光」要问清是哪一种，别把「关泛光」当「关所有光」
- **验证**：无头实测——默认方向光 1 个、设置里「太阳光」开关在、0 错误 ✓；部署健康 ✓

## 2026-08-16 13:33 · 日间/夜间加「自动」选项（三态循环）

- **需求**：日夜不只「日间/夜间」两态，要加「自动」（跟随太阳），底部栏和设置里都要
- **实现**：①BottomBar 日夜按钮改三态循环——自动(sunAuto=true) → 日间(night=false) → 夜间(night=true) → 自动；点击顺序切换，标签显示「自动/日间/夜间」；②设置「日间/夜间」改三态（日间/夜间/自动三个按钮，saveDefault 分别设 night/sunAuto）；③App.jsx settings 加载补 `sunAuto: settings.sunAuto !== false`
- **⚠️ 教训**：日夜三态 = `sunAuto`（自动跟随太阳）+ `night`（手动日夜）。「自动」是 sunAuto=true，「日间/夜间」是 sunAuto=false + night。三态循环要分清状态机
- **验证**：无头实测——底部栏含「自动」、设置里含「自动」和「日间/夜间」、0 错误 ✓；部署健康 ✓

## 2026-08-16 13:39 · 地板加厚后缝隙（膨胀消除）

- **用户反馈**：地板加厚后旋转有缝隙、不像一个整体
- **根因**：相邻房间地板边界有 0~8mm 吸附残留误差（detectRooms 容差吸附，阳台甚至 4cm），零厚度平面时贴地面看不出，加厚后有了侧面、缝隙就露出来
- **修复**：robustFloorGeometry 有厚度分支里，先「顶点沿中心外移 1cm」膨胀多边形，再 ExtrudeGeometry 挤出——相邻地板重叠、缝隙消除。1cm 远小于墙厚 12cm，不超出墙
- **⚠️ 教训**：加厚/挤出几何会暴露「边界不重合」的问题（平面时代被掩盖）。相邻房间地板不可能完美共线（吸附残留），要用「微膨胀让相邻重叠」来消除缝隙、造整体感
- **验证**：膨胀后 bbox 宽 3.017/深 2.011（原 3×2，外扩 1~1.7cm）、厚度 0.2 不变、无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 13:48 · 整体地板（消除房间边界，营造整体感）

- **用户反馈**：膨胀后还是「有界限、每个房间分一块」，不像一个整体
- **根因**：每个房间地板是独立的 mesh（独立挤出 + 独立半透明材质），相邻重叠处半透明叠加、侧面接缝，看起来一块一块
- **修复**：改成「整体地板」——geometry.js 加 `convexHull`（Andrew's monotone chain）+ `wholeFloorGeometry`（所有房间顶点凸包挤出一整块）；Scene 全部视图渲染整体地板（一个 mesh），房间单独视图（focusRoom）才用 Room 组件的单独地板（solo 配色）
- **⚠️ 教训**：相邻多边形「各自挤出 + 半透明」永远有边界/叠加，要「整体感」就做 union/凸包合成一整块，别用「膨胀重叠」凑合（重叠处半透明叠加反而更明显）
- **验证**：node 实测两个相邻 3×2 房合成整体 6×2 一整块、无内部边界；无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 14:04 · 撤销凸包整体地板 + 撤销膨胀（回到精确边界）

- **用户纠正**：凸包整体地板会把相邻房间凹角用「斜线」连起来形成打斜的地板，方法不靠谱；膨胀重叠又有半透明叠加/接缝
- **最终回退**：①撤销 wholeFloorGeometry 凸包整体地板（Scene 恢复每个房间独立地板）；②撤销 robustFloorGeometry 的膨胀，回到「每个房间 Shape、精确边界」
- **⚠️ 教训（血泪）**：①「相邻多边形各自挤出 + 半透明」的边界问题，用「膨胀重叠」或「凸包合并」都不是正解——膨胀=半透明叠加，凸包=凹角打斜；②正确的做法是对齐数据（相邻房间共享角点精确重合），参考竞品 blueprint3d：每个房间 Shape 从「共享角点」生成，角点 DCEL 共享，边界自然重合；③我方的「共享角点」其实有 5cm 错位（阳台画得不准 + SNAP_TOL 2cm 没吸附到），根源在数据，不在渲染
- **验证**：无头 0 错误 ✓；部署健康 ✓（地板回到精确边界，缝隙问题待数据对齐再彻底解决）

## 2026-08-16 14:09 · 修数据（吸附端点）+ 源头杜绝（画墙端点吸附）

- **用户要求**：两个都要——修复当前错位数据 + 从源头杜绝
- **修复数据**：geometry.js 加 `snapWallEndpoints(walls, tol=0.08)`（union-find 聚类，距离<8cm 的端点合并成质心）；对当前数据 mergeCollinearWalls → snapWallEndpoints → recomputeRooms，保存回后端。效果：相邻房间共享角点从 5cm 错位 → <1mm 对齐
- **源头杜绝**：①PlanEditor `wallSnapPoint` 改成「优先吸附端点」（光标<0.15m 内直接吸到端点，其次才是墙段中点吸附）——保证画墙时相邻墙共享角点精确重合；②画墙完成（closeDraft/addWallPoint 闭合）时先 snapWallEndpoints 再 mergeCollinearWalls 再 autoTrimWalls，画完自动吸附错位端点
- **⚠️ 教训**：数据错位（相邻墙端点 5cm 差）是「地板缝隙」的真正根源——之前一直在渲染层打补丁（膨胀/凸包）都错，正确是「数据对齐」+「源头吸附」。竞品 blueprint3d 没这问题是因为角点 DCEL 共享、天然重合
- **验证**：修复后相邻房间共线边端点差 <1mm、无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 14:20 · 拉直斜墙（彻底消除地板缝隙）

- **用户反馈**：端点对齐后还有问题
- **根因**：还有「斜墙」——一条长竖墙（6l5wj1ke）画斜了 0.19°（x 从 -2.206 斜到 -2.182，7.4m 长偏了 2.4cm），导致相邻房间地板边界 2cm 错位。端点吸附只对齐端点、不修方向
- **修复**：geometry.js 加 `straightenWalls`（几乎竖直的墙 x 对齐到平均、水平的墙 y 对齐到平均）；顺序改成 straightenWalls → snapWallEndpoints → mergeCollinearWalls → **straightenWalls（再拉直一次，消除两条独立墙合并后的残留斜）** → autoTrimWalls → recomputeRooms。同时修复当前数据
- **⚠️ 教训**：地板缝隙有「端点错位」和「墙方向斜」两个来源，端点吸附只管前者；斜墙要单独拉直。而且「先拉直→合并→再拉直」的顺序很关键——两条独立墙各自拉直后 x 不同，合并取端点又会斜，必须合并后再拉直一次
- **验证**：修复后所有相邻地板边界精确对齐（共享边 x 统一 -2.2、z 边界重合）、无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 14:32 · 地板强制不透明（消除「每个房间边界缝隙」）

- **用户反馈**：每个房间边界都有缝隙，且不是数据错位的原因
- **根因**：地板材质是**半透明**（opacity=0.25，用户之前设的）——相邻地板的半透明「侧面」在共享边处背靠背，半透明叠加/露边形成一圈「缝隙线」。这是渲染问题，不是数据错位
- **修复**：FloorMaterial 里地板强制不透明（`trans=false, op=1`，地板是实体不该半透明）
- **⚠️ 教训（血泪）**：排查「缝隙」要分清「几何错位」还是「材质半透明」——我先后试了端点吸附、拉直斜墙、凸包、膨胀，最后发现是地板半透明 opacity=0.25 导致的边界线。实体类物体（地板/墙）不该半透明，半透明会让相邻面的边界露出来
- **验证**：地板材质 transparent=false opacity=1、无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 14:59 · 方案 A：整块地板（矩形并集 union）+ 顶点颜色（半透明无缝 + 每房间配色 + 实心厚度）

- **用户需求**：要「整块地板」+「每房间独立配色」+「半透明无缝」+「实心厚度」
- **实现**：①geometry.js 用 `unionRectangles`（正交矩形并集，反复合并完美对齐的矩形，保留凹处、不打斜，替代之前的凸包）；②`wholeFloorGeometryWithColors`——顶面每个房间三角形涂各自房间的 color（顶点颜色），侧面/底面用 union 矩形挤出；③FloorMaterial 加 `vertexColors` 参数、恢复半透明（整体地板无内部侧面，半透明也不会叠加露边）；④Scene 全部视图渲染整体地板（一个 mesh），房间单独视图（focusRoom）才渲染单个房间地板
- **⚠️ 教训**：①「半透明 + 无缝」的正解是「整体 union（无内部侧面）」，不是凸包（凹角打斜）也不是膨胀（叠加）；②「整块地板 + 每房间配色」用「顶点颜色」——几何 union 成一块，颜色按房间涂在顶点上；③正交户型用「矩形并集」比通用多边形 union 简单可靠
- **验证**：node 实测矩形并集正确（相邻合并、L 形保留凹处）；无头实测地板 1 个 mesh、transparent+opacity 0.25、vertexColors+colorAttr、0 错误 ✓；部署健康 ✓

## 2026-08-16 15:03 · 修复地板「乱闪」（顶面 z-fighting）

- **用户反馈**：整块地板没缝了，但「乱闪」
- **根因**：整体地板里顶面有两层——「每个房间三角形」（y=0，顶点颜色）和「union 矩形 ExtrudeGeometry 挤出的顶面 cap」（也在 y=0），两层重叠 → z-fighting（深度冲突闪烁）
- **修复**：去掉 ExtrudeGeometry（它自带顶面 cap），改成手动生成——顶面用「每个房间三角形」（顶点颜色），底面用「union 矩形三角剖分」（y=-thickness），侧面用「外轮廓边」（共享 1 次的边才渲染，内部共享边跳过避免内部侧面）
- **⚠️ 教训**：ExtrudeGeometry 自带顶面+底面 cap，如果顶面另有「顶点颜色」的一层，就会重叠 z-fighting。要么手动只生成侧面+底面，要么接受 ExtrudeGeometry 的顶面、把顶点颜色做在它的顶面上
- **验证**：顶点数 48→42（去掉重叠顶面）、地板 1 个 mesh、vertexColors、0 错误 ✓；部署健康 ✓

## 2026-08-16 15:32 · 复刻米家 Xiaomi Home 3D 配色（3D 白天/夜间，2D 暂不动）

- **需求**：按米家给的 HEX 色号复刻 3D 场景配色，低饱和、柔和浅色、扁平化
- **改动**（全部在 Scene.jsx / geometry.js，2D 未动）：
  - THEME 墙色：day `#F0F0F0`（白墙）、night `#F8E9D3`（暖光墙）；地板色板改暖灰白系
  - SkyDome 天空：白天 `#E8F0F8`、夜间 `#2C3442`（扁平单色渐变）
  - 地面 `#EAE8E5`：地板 fallback + FLOOR_PALETTE + wholeFloorGeometryWithColors 的 defaultColor + room.color fallback（geometry.js）
  - 天花板 `#F7F7F7`（屋顶 fallback）
  - 灯光泛光 `#FFEBC2`（replace_all 原 `#ffd166`）
  - 夜间室内环境光 `#444448`（hemisphereLight night 分支）
  - 设备离线灰 `#999999`（DeviceMarker 加 offline 判断）；选中高亮/在线标识 `#3D88FF`（replace_all 原 `#2f7fe0`）
- **⚠️ 教训**：deploy_local.py 报「加载项没在运行」是**加载项刚重启还没 ready 的时序问题**（ingress_url 短暂为空），重试即可，别误判成真的没运行；curl ingress 能拉到 project.json 就说明在运行
- **验证**：无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 15:46 · 「米家模式」一键按钮

- **需求**：加一个「米家模式」按钮，点进去把全部换成米家配色（因为默认色改了但已有数据的地板色 #7789ad 是存进去的，不会自动变）
- **实现**：App.jsx 加 `applyMiHomeTheme`——清空每层 room 的 color/texture/solo*、墙的 color/opacity、楼层的 color，让它们回到米家默认浅色；设置面板「默认选项」tab 顶部加「米家模式」按钮「一键换成米家配色」
- **⚠️ 教训**：改「默认色」只影响新数据，已有数据里存的自定义颜色（color/texture/opacity）要单独清掉才能「一键换主题」——清字段用 delete 而不是赋默认值，让渲染层 fallback 生效
- **验证**：无头实测设置里「米家模式」「一键换成米家配色」在、0 错误 ✓；部署健康 ✓

## 2026-08-16 16:14 · 米家模式改成可开关（备份/恢复）

- **用户反馈**：米家模式一键清空后没法关、没关闭选项
- **实现**：store 加 `mihomeBackup`（开启前的 project 深拷贝）；`applyMiHomeTheme` 改成切换——开=深拷贝备份当前配色 + 清空成米家默认，关=按 id 从备份恢复 room 的 color/texture/solo*、墙的 color/opacity、楼层的 color；按钮文案随状态变「一键换成米家配色」↔「关闭（恢复原配色）」
- **⚠️ 教训**：「一键换主题/清空颜色」这类破坏性操作要做成可逆——备份原值（深拷贝），提供恢复；别只做「开」不做「关」
- **验证**：无头实测开关切换正常（开→关按钮出现、关→开按钮恢复）、0 错误 ✓；部署健康 ✓

## 2026-08-16 16:22 · 米家模式改成渲染层开关（开/关、默认开、持久化）

- **用户反馈**：按钮要简洁「开/关」；刷新后状态保存不上
- **根因**：之前用「备份/恢复」——备份存内存 mihomeBackup，刷新就丢，而且「关」无法恢复
- **重构**：改成「渲染层开关」——store 加 `mihomeMode`（默认 true），存 settings 持久化；Scene 渲染时 mihomeMode 覆盖地板 #EAE8E5 / 墙 #F0F0F0 / 屋顶 #F7F7F7（不 vertexColors），**不改数据**；App 按钮「开/关」toggle + saveSettings；settings 加载 `mihomeMode !== false`（默认开）
- **⚠️ 教训**：主题/配色切换应该用「渲染层开关 + 存 settings」，别用「改数据 + 备份」（备份存内存刷新就丢、不可逆）。渲染覆盖才是可逆、可持久化的正解
- **验证**：无头实测按钮显示「关」（默认开）、0 错误 ✓；部署健康 ✓

## 2026-08-16 16:29 · 米家模式按钮改「开/关」两键 + 恢复被误删的颜色

- **用户反馈**：别的开关都是「开」「关」两个按钮，米家模式不该一个按钮切换；且切「关」后刷新没恢复原配色
- **修复**：①按钮改成「开」「关」两个按钮（`setMiHome(true/false)` + `seg()` 高亮，跟其他 toggle 一致）；②恢复数据——之前「清空颜色」误删了用户房间色 #7789ad、墙色 #ffffff/opacity10，已用脚本写回 project.json；③settings.json 里残留的 `mihomeMode:false`（测试点「关」存的）导致「默认开」失效，改成 true
- **⚠️ 教训（血泪）**：①「清空颜色做主题切换」是破坏性的——会删掉用户自定义颜色且不可逆，正确是「渲染层开关」；②改了默认值后要查 settings.json 里有没有「测试残留的旧值」把默认值覆盖掉（`settings.x !== false` 遇到存的 false 就失效）；③同类控件（开关）要和已有的用同一套 UI（两键 toggle），别自己发明一个按钮
- **验证**：无头实测——按钮 [开,关] 两键、默认开地板 #eae8e5、关后顶点色恢复 #7789ad、0 错误 ✓；部署健康 ✓

## 2026-08-16 16:34 · 米家模式补「透明度」覆盖

- **用户反馈**：米家模式开了还是透明的（没变化）
- **根因**：米家模式只覆盖了「颜色」（地板/墙/屋顶的 color），没覆盖「透明度」——用户之前设的地板 opacity 25、墙 opacity 10 还生效，所以还是半透明
- **修复**：Scene 渲染处米家模式开时同步覆盖透明度——地板 opacity 100（不透明）、墙 opacity 0.197（米家毛玻璃），不再受数据里的 opacity 影响
- **⚠️ 教训**：主题/配色覆盖要「颜色 + 透明度」一起覆盖，不能只改色不管透明度；验证「默认开」别用会点「关」的脚本（点了会把 settings 存成 false，污染下一次）
- **验证**：无头实测——米家开时地板 opacity=1、白墙 opacity=0.197（11 面）、0 错误 ✓；部署健康 ✓

## 2026-08-16 17:03 · 渐变背景色保存不上（漏了加载）

- **用户反馈**：设置里改的渐变背景色，刷新后又消失
- **根因**：`bgGradient1/bgGradient2` 保存了（saveSettings 里写了），但 App.jsx 的 settings 加载处**漏了读这两个字段**，刷新后 store 用默认值 #253962/#46618d，把用户改的盖掉
- **修复**：settings 加载处补 `bgGradient1: settings.bgGradient1 || '#253962'`、`bgGradient2: settings.bgGradient2 || '#46618d'`
- **⚠️ 教训**：新增一个「可持久化」的设置项，要「保存 + 加载」成对出现，漏一边就是「改了但刷新丢」。排查「保存不上」先看加载处有没有读这个字段
- **验证**：无头 0 错误 ✓；部署健康 ✓

## 2026-08-16 17:14 · 「原版玻璃」开关（meshPhysicalMaterial + clearcoat）

- **用户反馈**：透明墙没有 3D Home（JMGLink 原版）那种辨识度、看着得劲，问差在哪
- **根因（差在哪）**：原版毛玻璃墙是 `meshPhysicalMaterial`（roughness 0.5 / metalness 0 / **clearcoat 0.16 / clearcoatRoughness 0.72** / depthWrite false），而我用的是 `meshStandardMaterial`（无 clearcoat）——**差的正是 clearcoat 清漆层**，那层光泽反射就是「辨识度」的来源
- **修复**：store 加 `glassMode`（默认开）；WallMaterial 加 glassMode 分支——开时用 meshPhysicalMaterial + clearcoat 0.16 + clearcoatRoughness 0.72（对齐原版），关时用回 meshStandardMaterial；设置面板加「原版玻璃」开/关两键（跟米家模式放一块）
- **⚠️ 教训**：玻璃/毛玻璃的「质感/辨识度」来自 clearcoat（清漆层）——meshStandardMaterial 没有 clearcoat，要 meshPhysicalMaterial；之前「对齐原版」的结论就在开发日志里（156 行），新会话别丢掉再猜
- **验证**：无头实测——透明墙 11 个全是 meshPhysicalMaterial + clearcoat=0.16、设置里有「原版玻璃」、0 错误 ✓；部署健康 ✓

## 2026-08-16 17:20 · 深挖原版：玻璃辨识度不是 clearcoat，是「磨砂米白 + 几乎不透明」

- **用户反馈**：开原版玻璃和不开没区别，之前的 clearcoat 方向错了，要深挖原版代码
- **深挖结果**（`~/ha-dashboard/3d-home-addon/.../webui/assets/app.js`）：原版墙是 **`meshStandardMaterial`，根本没有 clearcoat/transmission**！真实参数：
  `color: night ? 0x2c3a52 : 0xf5f2ec`（米白 #f5f2ec，暖白不是冷灰蓝）+ `roughness: 0.85`（磨砂）+ `metalness: 0` + `transparent: true, opacity: 0.96`（几乎不透明）
- **纠正之前错误**：开发日志 156 行记的「毛玻璃 = 半透明 0.2 + clearcoat 0.16」是**错的**（当时没读原版代码猜的）。原版墙的「辨识度」来自「米白 #f5f2ec + 磨砂 roughness 0.85 + 几乎实心 opacity 0.96」
- **修复**：WallMaterial 的 glassMode 分支改成 meshStandardMaterial + roughness 0.85 + opacity 0.96 + metalness 0；墙色 glassMode 优先 #f5f2ec、透明度 0.96
- **⚠️ 教训（血泪）**：对齐原版 UI/材质**必须直接读原版源码**（app.js 就 47K，grep 材质参数几秒就出），别靠「毛玻璃」「玻璃」这种印象猜参数——之前猜 clearcoat 猜错了一整轮。原版 app.js 在 `3d-home-addon/3d_home/rootfs/usr/local/bin/webui/assets/app.js`（本地有，新会话可直接 grep）
- **验证**：无头实测——原版玻璃墙 11 个全是 roughness=0.85 / opacity=0.96 / #f5f2ec、0 错误 ✓；部署健康 ✓

## 2026-08-16 17:27 · 再深挖：玻璃质感 = glass 风格（clearcoat 0.16 + #d5e0f1 + opacity 0.24）

- **用户反驳**：实物就在面前，墙「就是玻璃质感」，不是米白磨砂
- **根因（这次才对）**：之前深挖到了「旧版 app.js（3d-home-addon 47K）」的墙合并材质（米白 #f5f2ec + opacity 0.96），那是**旧版本**；用户看到的「玻璃」是「新版（3d-home-fixed 1.8M 的 index-DYPp8s_r.js）」的 **glass 视觉风格**
- **原版 glass 风格墙真实参数**（`qh` 函数，presentation 模式）：
  `meshPhysicalMaterial({ color: day #d5e0f1 / night #d0dcee, roughness: 0.5, metalness: 0, clearcoat: 0.16, clearcoatRoughness: 0.72, transparent: true, opacity: 0.24 })`
- **⚠️ 教训（血泪）**：原版有多个视觉风格（glass/cartoon/cream，`visualStyle`），墙色/透明度/材质都不同——glass 是 #d5e0f1+#d0dcee+opacity0.24+clearcoat0.16，cream 是 #f4efe5+opacity0.86。**要挖就挖「正在运行的版本」的 bundle（3d-home-fixed 的 index 1.8M），别挖旧版 3d-home-addon 的 47K app.js**；两个仓库同名但内容不同代
- **验证**：无头实测——玻璃墙 11 个全是 clearcoat=0.16 / roughness=0.5 / opacity=0.24 / #d5e0f1、0 错误 ✓；部署健康 ✓

## 2026-08-16 17:37 · 玻璃质感精髓 = 墙材质 + 深蓝灰背景（透出深色才有玻璃感）

- **用户反馈**：墙还是不对劲、没挖到精髓，视觉就是不一样
- **再深挖原版 qh 墙渲染函数**：墙 opacity 其实是 `Math.max(.2, wallOpacity*.82)=0.2`（不是 0.24）；关键在「玻璃墙透出的背景」——原版 glass 风格背景是深蓝灰 `#5278ae`，玻璃墙半透明透出这层深色才有玻璃感
- **修复**：①WallMaterial glassMode opacity 0.24→0.2；②SceneBackground 加 glassMode 分支——原版玻璃时 scene.background 强制 `#5278ae`（深蓝灰天空，玻璃墙透出深色）
- **⚠️ 教训**：玻璃/半透明材质的「质感」一半靠材质（clearcoat/roughness/opacity），一半靠**它透出的背景色**——浅色背景透出浅色、玻璃感弱，深蓝灰背景透出深色、玻璃感才明显。做「复刻」要把「材质 + 背景」当成一个整体一起还原，不能只调材质
- **验证**：无头实测——背景 #5278ae、玻璃墙 11 个 clearcoat=0.16/opacity=0.2/#d5e0f1、0 错误 ✓；部署健康 ✓
